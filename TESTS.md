# Каталог тестов — Стенограф

> ⚙️ **Сгенерирован автоматически** из кода тестов (`python scripts/test_inventory.py`), руками не редактируется.
> Обновлён: 05.09.2026 19:36

Всего тестовых функций на сервере: **182** (с учётом параметризации — 195 проверок). Клиент: **97**.

Здесь написано, **что именно** проверяет каждый тест. Покрытие кода (сколько строк исполнилось) — отдельная метрика, она в [TEST_REPORT.md](TEST_REPORT.md).

## Оглавление

- **Юнит** — 144 тестов
  - [`test_auth.py`](#test-authpy) — токены: хеширование, заголовок, закрытые пути, заведение людей (26)
  - [`test_bench_registry.py`](#test-bench-registrypy) — замер диаризации на эталоне голосов (5)
  - [`test_config.py`](#test-configpy) — конфигурация и персист выбора движка ASR (6)
  - [`test_crud.py`](#test-crudpy) — операции с БД: встречи, спикеры, сегменты (8)
  - [`test_deploy_script.py`](#test-deploy-scriptpy) — скрипт обновления сервера: POSIX-совместимость (3)
  - [`test_device.py`](#test-devicepy) — выбор устройства для моделей: cuda/mps/cpu и откат (8)
  - [`test_isolation.py`](#test-isolationpy) — разделение по владельцам: чужая встреча не видна и не правится (6)
  - [`test_live_session.py`](#test-live-sessionpy) — живая сессия: разрез сегментов по смене говорящего (8)
  - [`test_loadtest_metrics.py`](#test-loadtest-metricspy) — разбор метрик нагрузочного прогона (7)
  - [`test_merge_live.py`](#test-merge-livepy) — слияние профилей во время встречи и уведомление владельцу (5)
  - [`test_mixer.py`](#test-mixerpy) — микшер каналов: склейка mic/system и определение доминанты (10)
  - [`test_registry.py`](#test-registrypy) — диаризация: сопоставление голосов, приоры, слияние профилей (23)
  - [`test_search.py`](#test-searchpy) —  (6)
  - [`test_search_vectors.py`](#test-search-vectorspy) — векторы от клиента: что индексировать, хранение, подбор ближайших (7)
  - [`test_summary_intake.py`](#test-summary-intakepy) — приём готового протокола от приложения и ошибки генерации (6)
  - [`test_transcriber_junk.py`](#test-transcriber-junkpy) — фильтр галлюцинаций ASR на тишине (5)
  - [`test_vad.py`](#test-vadpy) — нарезка речи по паузам, отбрасывание коротких фрагментов (5)
- **Компонентные (API-слой)** — 16 тестов
  - [`test_api.py`](#test-apipy) — REST-эндпоинты: коды ответов, форма данных, отказы (16)
- **Интеграционные (БД)** — 8 тестов
  - [`test_migrations.py`](#test-migrationspy) — апгрейд старых баз без потери данных (8)
- **Интеграционные (модели)** — 3 тестов
  - [`test_diarization_integration.py`](#test-diarization-integrationpy) — реальная модель ECAPA на синтезе голосов macOS (3)
- **E2E (системные)** — 10 тестов
  - [`test_e2e_live.py`](#test-e2e-livepy) — сквозные сценарии встречи через WebSocket (10)
- **Нагрузочные** — 1 тестов
  - [`test_load_smoke.py`](#test-load-smokepy) — дымовой нагрузочный прогон (1)
- **Клиент (Vitest)** — 97 тестов
  - [`live.test.ts`](#livetestts)
  - [`rest.test.ts`](#resttestts)
  - [`capture.test.ts`](#capturetestts)
  - [`Transcript.test.ts`](#Transcripttestts)
  - [`meeting.e2e.test.ts`](#meetinge2etestts)
  - [`hints.test.ts`](#hintstestts)
  - [`ollama.test.ts`](#ollamatestts)
  - [`openai.test.ts`](#openaitestts)
  - [`port.test.ts`](#porttestts)
  - [`search.test.ts`](#searchtestts)
  - [`settings.test.ts`](#settingstestts)
  - [`similarity.test.ts`](#similaritytestts)
  - [`summary.test.ts`](#summarytestts)
  - [`store.test.ts`](#storetestts)

## Юнит

*функция или класс в изоляции, зависимости замоканы*

### `test_auth.py`

Путь: `server/tests/test_auth.py`  
Токены доступа: кого сервер пускает и что отдаёт чужому. Тесты чистят за собой людей в finally не из аккуратности, а по необходимости: база у функциональных тестов одна на весь прогон, и оставленный пользователь включил бы обязательный токен для всех остальных тестов сразу.

| Тест | Что проверяет |
|---|---|
| `test_ws_без_кадра_auth_закрывается` | Первым кадром пришло аудио, а не токен — соединение не начинается. |
| `test_ws_без_людей_пускает_без_токена` | ws без людей пускает без токена |
| `test_ws_молчание_обрывается_по_таймауту` | Молчащее соединение не висит вечно: иначе их пачкой занимают сервер, не зная токена. Таймаут в тесте укорочен, чтобы не ждать десять секунд. |
| `test_ws_повторный_auth_не_ошибка` | Клиент может прислать auth ещё раз — это не «неизвестная команда». |
| `test_ws_с_пустым_токеном_отвечает_сразу` | Клиент шлёт кадр auth даже без токена — чтобы отказ пришёл сразу, а не через десять секунд ожидания, пока человек уже говорит. |
| `test_ws_с_чужим_токеном_закрывается` | ws с чужим токеном закрывается |
| `test_ws_со_своим_токеном_работает` | ws со своим токеном работает |
| `test_автодокументация_закрыта` | Под /api она не попадает, но карту сервера постороннему отдавать незачем. |
| `test_без_людей_пускает_без_токена` | без людей пускает без токена |
| `test_без_токена_401` | без токена 401 |
| `test_второй_человек_чужого_не_получает` | Усыновление разовое: иначе каждый новый сотрудник забирал бы себе всё, что успело осиротеть. |
| `test_двое_через_http_видят_только_своё` | Тот же запрет, но через реальный конвейер: middleware → эндпоинт → БД. |
| `test_закрыты_и_настройки` | Не только чтение встреч: смена провайдера LLM чужому тоже недоступна. |
| `test_здоровье_без_токена_урезано` | здоровье без токена урезано |
| `test_здоровье_с_токеном_полное` | здоровье с токеном полное |
| `test_здоровье_со_слэшем_тоже_открыто` | «/api/health/» — тот же health: мониторинг с привычным слэшем не должен видеть упавший сервер вместо живого. |
| `test_первый_человек_получает_ничейные_встречи` | Полгода работы на личном сервере не должны исчезнуть в момент, когда его закрывают токеном: записи на месте, но не видны никому — это выглядит как потеря архива, а не как защита. |
| `test_повторное_удаление_не_роняет_сервер` | Проверка прав и удаление — в разных сессиях, между ними встречу могли удалить (двойной клик, два клиента). Второй запрос должен ответить, а не упасть на None. |
| `test_разбор_заголовка` | разбор заголовка _(×8 кейсов)_ |
| `test_свой_токен_пускает` | свой токен пускает |
| `test_список_встреч_только_свои` | список встреч только свои |
| `test_токен_хранится_хешем` | токен хранится хешем |
| `test_токены_разные` | токены разные |
| `test_чужая_встреча_неотличима_от_несуществующей` | 404, а не 403: по разнице между ними перебором узнаётся, сколько встреч у соседа и когда они шли. |
| `test_чужие_голоса_через_http_недоступны` | Библиотека голосов у каждого своя — включая переименование и удаление. Отпечаток голоса это биометрия: без проверки владельца её забрал бы любой, кто подставит номер профиля. |
| `test_чужой_токен_401` | чужой токен 401 |

### `test_bench_registry.py`

Путь: `server/tests/test_bench_registry.py`  
Бенчмарк роста библиотеки голосов (scripts/bench_registry.py). Тесты не меряют производительность (это дело самого бенчмарка), а проверяют, что он меряет ТО, что заявляет: полный перебор отпечатков без обращения к БД. Иначе бенчмарк тихо перестанет отражать реальность, и вывод «диаризация не узкое место» окажется основан на сломанном замере.

| Тест | Что проверяет |
|---|---|
| `test_embeddings_are_unit_vectors` | match_all считает скалярное произведение как косинусную близость — значит векторы обязаны быть нормированы, иначе пороги теряют смысл. |
| `test_fill_creates_requested_library` | fill creates requested library |
| `test_measure_does_not_touch_real_db` | Заглушка БД обязана перехватывать все обращения: если бенчмарк однажды начнёт писать в рабочую базу, это заметят слишком поздно. |
| `test_measure_reports_linear_growth` | Главная проверка: время растёт пропорционально размеру библиотеки. Если кто-то заменит перебор на индекс, тест упадёт — и это будет правильный сигнал обновить выводы в LOAD_REPORT.md, а не молча тащить старые. |
| `test_probe_matches_existing_speaker` | Проба должна УЗНАВАТЬСЯ: со случайным вектором match_all пошла бы заводить нового спикера, и замер мерил бы не перебор, а запись в БД. |

### `test_config.py`

Путь: `server/tests/test_config.py`  
Юнит-тесты конфигурации (config.py): дефолты и персист выбора ASR-движка.

| Тест | Что проверяет |
|---|---|
| `test_defaults` | Дефолты конфига соответствуют заявленным в README (движок ASR, пути к данным). |
| `test_save_and_load_asr_choice` | Выбор движка и модели ASR переживает перезапуск: пишется в asr.json и читается обратно. |
| `test_версия_из_окружения` | версия из окружения |
| `test_версия_по_умолчанию_dev` | версия по умолчанию dev |
| `test_короткая_версия_в_api` | Сорок символов в строке состояния читать невозможно, семи хватает, чтобы найти коммит. _(×4 кейсов)_ |
| `test_старые_переменные_llm_не_роняют_запуск` | Настройки моделей уехали на клиент, а строки в .env у людей остались. Без extra="ignore" сервер вообще не поднимается: pydantic падает при импорте на незнакомой переменной с нашим префиксом. У Сергея это .env, на машине деплоя — STENOGRAF_OLLAMA_URL в docker-compose; и то и другое пережило бы обновление образа, а сообщение «Extra inputs are not permitted» не подсказывает, что делать. |

### `test_crud.py`

Путь: `server/tests/test_crud.py`  
Юнит-тесты операций с БД (db/crud.py) на in-memory SQLite (фикстура db_session).

| Тест | Что проверяет |
|---|---|
| `test_add_segment_and_list_meetings` | Список встреч отдаёт название, число реплик и признак наличия резюме. |
| `test_create_and_end_meeting` | Встреча создаётся в статусе live с фолбэком названия; завершение проставляет время один раз и повторно его не сдвигает. |
| `test_create_speaker_default_name` | Новому спикеру даётся имя-заглушка «Спикер N» и он не помечается владельцем. |
| `test_list_speakers_counts_and_order` | Владелец «Вы» идёт первым в списке; у каждого спикера считаются реплики, встречи и число отпечатков голоса. |
| `test_meeting_segments_ordered_and_dict` | Реплики возвращаются по возрастанию времени; в словаре близость округлена до 3 знаков, а спикер может быть None. |
| `test_reassign_segments` | Перенос реплик между спикерами возвращает число перенесённых; повторный перенос уже ничего не двигает. |
| `test_rename_speaker` | Переименование обрезает пробелы по краям и сохраняется в БД; для несуществующего id возвращается None. |
| `test_self_speaker_created_once` | Профиль владельца «Вы» создаётся один раз: повторный вызов возвращает тот же id, а не плодит дубли. |

### `test_deploy_script.py`

Путь: `server/tests/test_deploy_script.py`  
Скрипты в deploy/ обязаны работать в dash, а не только в bash. Поймано 04.09.2026 на живом сервере: update.sh упал первой же строкой с «КАТАЛОГ=/opt/stenograf: not found». На Ubuntu /bin/sh — это dash, и имена переменных там только ASCII; на маке /bin/sh — bash, который кириллицу терпит, поэтому локальная проверка `sh -n` прошла и дала ложную уверенность. Тест живёт среди серверных, потому что другого набора в проекте нет, а проверять надо: скрипт запускается ровно один раз в жизни каждой версии — на сервере, руками, и обнаруживается сломанным в самый неподходящий момент.

| Тест | Что проверяет |
|---|---|
| `test_имена_переменных_ascii` | имена переменных ascii |
| `test_синтаксис_в_dash` | Настоящая проверка тем самым интерпретатором. Пропускается, если dash не установлен — тогда остаётся проверка имён выше, которая ловит тот же класс ошибок без него. |
| `test_скрипты_нашлись` | Если каталог переименуют, тест должен упасть, а не тихо проверять пустоту. |

### `test_device.py`

Путь: `server/tests/test_device.py`  
Выбор вычислительного устройства. Замечание куратора №4: у него на Windows всё считалось процессором при живой видеокарте, потому что "cpu" было зашито в трёх загрузчиках. Здесь проверяется только логика выбора — саму видеокарту в тестах не потрогать.

| Тест | Что проверяет |
|---|---|
| `test_auto_does_not_take_metal_by_itself` | Metal сам не выбирается: GigaAM и ECAPA на нём не мерялись, а на процессоре мерялись и работают быстрее реального времени. Для whisper на Apple Silicon в проекте есть отдельный движок mlx. |
| `test_auto_falls_back_to_cpu_without_a_card` | auto falls back to cpu without a card |
| `test_auto_prefers_the_graphics_card` | auto prefers the graphics card |
| `test_broken_torch_is_treated_as_no_gpu` | Импорт torch может упасть (битая установка, неполный образ) — это «нет видеокарты», а не повод не запустить сервер. |
| `test_missing_device_falls_back_instead_of_crashing` | Конфиг переезжает с машины на машину: у Сергея Mac, у куратора Windows с видеокартой, на сервере организации может не быть ни того, ни другого. |
| `test_nonsense_value_is_not_fatal` | nonsense value is not fatal |
| `test_precision_follows_the_device` | int8 на видеокарте не даёт ничего, кроме потери качества. |
| `test_whisper_never_gets_metal` | faster-whisper работает через CTranslate2, а тот знает только cpu и cuda: отдав ему mps, получили бы падение при загрузке модели. |

### `test_isolation.py`

Путь: `server/tests/test_isolation.py`  
Сводная проверка изоляции: что закрыто токеном и что не видно чужому. Отдельным файлом от test_auth.py намеренно. Там проверяется механика токена (разбор заголовка, кадр auth, хранение хешем), здесь — обещание, которое сервер даёт человеку: «у каждого своё приложение, пересечений нет». Обещание проверять надо целиком и по всем эндпоинтам сразу, иначе новый эндпоинт добавят, а проверку к нему — нет. Главный тест здесь — перебор ВСЕХ маршрутов приложения. Он ловит не то, что написано сегодня, а то, что допишут завтра: забытый токен у нового эндпоинта проваливает прогон, и это единственная защита, которая не зависит от того, вспомнил ли автор про доступ.

| Тест | Что проверяет |
|---|---|
| `test_каждый_маршрут_закрыт_без_токена` | Ни один маршрут, кроме здоровья, не отвечает без токена. Перебор по маршрутам приложения, а не по списку в тесте: список пришлось бы дописывать руками, и первый же забытый эндпоинт остался бы открытым молча. |
| `test_личный_сервер_открыт_как_раньше` | Пока людей не заводили, сервер работает без токена — иначе локальный запуск потребовал бы настройки на пустом месте. |
| `test_поиск_не_видит_чужих_встреч` | Фильтр владельца стоит и в выдаче, и в списке на индексацию: чужую встречу нельзя ни найти, ни увидеть в очереди на подсчёт векторов. |
| `test_схема_api_тоже_закрыта` | Схема — это карта сервера. Отдавать её тому, от кого сервер закрывали, незачем: она перечисляет все эндпоинты и форму их тел. |
| `test_чужая_встреча_не_найдена` | 404, а не 403: разница между ними — это ответ на вопрос «а есть ли у соседа встреча с таким номером», то есть утечка без единого байта текста. _(×4 кейсов)_ |
| `test_чужой_голос_не_найден` | чужой голос не найден |

### `test_live_session.py`

Путь: `server/tests/test_live_session.py`  
Тесты правил LiveSession, влияющих на то, кому припишется реплика: короткий сегмент («да», «угу») без эмбеддинга приписывается последнему говорившему, но только в течение 4 секунд и только из того же канала; сегмент со сменой доминанты канала режется на части.

| Тест | Что проверяет |
|---|---|
| `test_first_replica_of_a_meeting_is_not_a_new_speaker` | Первая же реплика встречи — короткая: раньше она заводила спикера. Донора ещё нет по определению, и «алло» на старте становилось «Спикером 1» с отпечатком голоса, собранным по обрывку. Такой отпечаток потом путал диаризацию всей встречи. |
| `test_short_segment_after_long_pause_stays_unattributed` | Пауза больше 4 с — прилипания нет, но и опознавать нечего. Замечание куратора №13: раньше такой обрывок шёл в эмбеддер и заводил нового «Спикера N» на каждое «ага». На 0.2 секунды эмбеддер считает не голос, а что придётся, поэтому реплика остаётся ничьей. |
| `test_short_segment_from_other_channel_stays_unattributed` | Быстрое «да» из звонка сразу после фразы владельца — другой человек. Наследовать нельзя (это не владелец), опознавать не по чему (0.2 секунды), поэтому реплика остаётся ничьей вместо выдуманного нового участника. |
| `test_short_segment_inherits_self` | Короткая реплика после фразы владельца наследует и флаг «Вы». |
| `test_short_segment_reuses_last_speaker` | Короткое «угу» сразу после реплики — тот же человек, эмбеддинг не считается. |
| `test_split_by_dominance_cuts_segment` | Сегмент со сменой канала посередине режется на две части: аудио не теряется, граница — в точке смены доминанты. |
| `test_unattributed_replica_does_not_become_a_donor` | Ничьим обрывком следующие короткие реплики не приписываются. Иначе одна неопознанная реплика тянула бы за собой цепочку таких же — приписанных неизвестно кому. |
| `test_unattributed_replica_leaves_no_voiceprint` | Ничья реплика не попадает в базу голосов — эмбеддер к ней не звали. |

### `test_loadtest_metrics.py`

Путь: `server/tests/test_loadtest_metrics.py`  
Метрики нагрузочного харнесса (scripts/loadtest.py). Быстрые юнит-тесты чистых функций: ни сервера, ни моделей, ни Docker. Появились после того, как в отчёте обнаружился p95 МЕНЬШЕ медианы — наивная формула индекса на малых выборках брала минимум вместо максимума.

| Тест | Что проверяет |
|---|---|
| `test_degraded_catches_silent_transcript_loss` | Самый коварный отказ: встречи завершились штатно, но часть без транскрипта. На замерах явные ошибки плавали (18 отказов и 0 в повторе той же ступени), а недостача сегментов воспроизводилась стабильно — значит ловить надо её. |
| `test_degraded_flags_container_death` | Перезапуск контейнера и OOM считаются отказом, даже если ошибок встреч нет. |
| `test_degraded_flags_errors_and_latency` | Ошибки встреч, отсутствие распознавания и превышение p95 — тоже отказ. |
| `test_degraded_without_container_metrics` | Прогон против своего сервера: полей контейнера нет — не должно падать. |
| `test_p95_never_below_median` | Главная гарантия: 95-й перцентиль не может оказаться ниже медианы. |
| `test_p95_on_tiny_sample_takes_worst` | Регрессия: при двух замерах брался минимум, из-за чего p95 < p50. |
| `test_percentile_bounds` | Индекс не вылезает за границы списка ни при каком квантиле. |

### `test_merge_live.py`

Путь: `server/tests/test_merge_live.py`  
Объединение спикеров ПОСРЕДИ идущей встречи. Окно разговора уехало в приложение вместе с подсказками, поэтому проверки про переименование строк «Имя: текст» отсюда удалены — их место теперь в клиенте. Осталось то, что относится к диаризации: донор коротких реплик, недавний говоривший и счётчик участников. Слияние приходит по REST (страница «Спикеры» открыта окном поверх встречи), а живая сессия держит id спикеров в памяти. Профиль-источник при этом удаляется из базы, и без уведомления сессия продолжала бы им пользоваться. Опасность тихая: PRAGMA foreign_keys в SQLite по умолчанию выключен и в проекте не включается, поэтому реплика, приписанная удалённому спикеру, не упала бы с ошибкой — она молча записалась бы ссылкой в никуда и не нашлась бы ни у одного участника.

| Тест | Что проверяет |
|---|---|
| `test_merging_a_speaker_with_itself_changes_nothing` | Защита от вырожденного случая: считать 12 реплик дважды нельзя. |
| `test_notify_reaches_only_running_meetings` | Рассылка идёт по живым сессиям; завершённая её не получает. |
| `test_participants_do_not_split_in_two` | Счётчик реплик складывается: иначе модель увидит лишнего участника. |
| `test_recent_prior_forgets_the_deleted_speaker` | Приор «кто говорил недавно» тоже чистится — id уже не существует. |
| `test_short_replica_goes_to_the_surviving_speaker` | Донор коротких реплик перестаёт указывать на удалённый профиль. Иначе следующее «ага» уехало бы на спикера, которого больше нет. |

### `test_mixer.py`

Путь: `server/tests/test_mixer.py`  
Тесты микшера каналов: выравнивание потоков, паддинг отстающего канала, RMS-доминанта для диаризации.

| Тест | Что проверяет |
|---|---|
| `test_clipping_protection` | Сумма громких каналов не выходит за пределы диапазона (нет клиппинга). |
| `test_dominance_mic_system_mixed` | Когда оба канала звучат сопоставимо громко, доминанта помечается как mixed. |
| `test_dominance_single_active_channel` | Если звучит только один канал, он и объявляется доминантой. |
| `test_dominance_spans_ignores_short_blip` | Кашель в микрофон (одно окно) посреди чужой фразы — не смена говорящего. |
| `test_dominance_spans_single_channel` | Второго канала нет — резать не по чему. |
| `test_dominance_spans_split_on_channel_flip` | Ответ без паузы: 1.2 с из звонка, потом 1.2 с в микрофон — сегмент делится на два участка ровно в точке смены канала. |
| `test_flush_drains_remainder` | Финальный flush отдаёт остаток буфера — хвост встречи не теряется. |
| `test_lagging_channel_padded_and_late_samples_dropped` | Канал отстал больше max_lag — доигрываем с тишиной, а опоздавшие семплы отбрасываем, чтобы шкала времени не разъехалась. |
| `test_single_channel_passthrough` | Системный источник выключен — после стартовой паузы (ждём второй канал не дольше max_lag) микрофон идёт как есть. |
| `test_two_channels_are_summed_aligned` | Каналы приходят разными чанками — на выходе поэлементная сумма. |

### `test_registry.py`

Путь: `server/tests/test_registry.py`  
Юнит-тесты SpeakerRegistry — логики «кто говорит». Вместо реальных голосов используются синтетические 192-мерные векторы с точно заданной косинусной близостью: так проверяется именно решающая логика (порог, дрейф центроида, merge), а не качество ECAPA.

| Тест | Что проверяет |
|---|---|
| `test_above_threshold_matches` | Похожий выше порога голос узнаётся как тот же человек. |
| `test_below_threshold_creates_new_profile` | Голос, непохожий сильнее порога, заводит отдельный профиль, а не приписывается чужому. |
| `test_borderline_short_segment_leaves_no_print` | Пограничный коротыш («ага», узнанное со скидкой) не рождает отпечаток и не двигает существующий — источник мусорных отпечатков закрыт. |
| `test_centroid_drifts_toward_recent_voice` | Отпечаток — скользящее среднее: после серии реплик он смещается к текущему звучанию голоса (человек охрип, сменил гарнитуру). |
| `test_first_mic_voice_enrolls_self` | Первый голос, доминирующий в микрофоне, — владелец: профиль «Вы» обучается автоматически, без отдельной записи образцов. |
| `test_first_voice_creates_new_profile` | Первый услышанный голос создаёт новый профиль спикера. |
| `test_forget_removes_profile` | Удалённый профиль убирается из памяти реестра и больше не участвует в сопоставлении. |
| `test_merge_moves_prints_and_segments` | Один человек распознался как два профиля → merge переносит отпечатки и реплики, лишний профиль удаляется, данные не теряются. |
| `test_print_audio_saved_and_removed` | «Звучание» сохраняет аудио своей реплики (его можно прослушать); удаление отпечатка удаляет и файл. |
| `test_prints_capped_per_speaker` | Отпечатков на человека не больше speaker_max_prints — дальше «звучания» вливаются в ближайший отпечаток. |
| `test_recent_speaker_bonus_glues_monologue` | Монолог из звонка: близость просела чуть ниже порога, но спикер говорил только что — скидка недавности не даёт наплодить «Спикер N». |
| `test_remove_print_forgets_voice` | Удаление отпечатка: голос перестаёт узнаваться, профиль остаётся; несуществующий отпечаток — честный отказ. |
| `test_same_voice_matches_same_profile` | Повторное появление того же голоса попадает в уже существующий профиль. |
| `test_second_person_at_mic_gets_new_profile` | Два человека за одним микрофоном: непохожий голос из микрофона не приклеивается к «Вы», а получает свой профиль. |
| `test_self_bonus_keeps_owner_below_threshold` | Живой голос владельца гуляет: близость чуть ниже порога, но голос из микрофона — скидка speaker_self_bonus удерживает его в «Вы», и другое «звучание» добавляется вторым отпечатком. |
| `test_self_print_updates_only_from_mic` | Отпечаток «Вы» дообучается только голосом из микрофона: звук из звонка (даже похожий) не должен размывать профиль владельца. |
| `test_similar_voices_merge_at_low_threshold` | Обратная сторона низкого порога: голоса с близостью выше порога считаются одним человеком. При threshold=0.35 два похожих голоса (близость 0.5 — бывает у людей одного пола и тембра) склеятся. |
| `test_three_distinct_voices_stay_separate` | Три человека с непохожими голосами: реплики вперемешку, профилей — три. Случайные векторы в 192 измерениях почти ортогональны (близость ~0), у реальных непохожих голосов она 0.1–0.3 — здесь моделируется удобный случай. |
| `test_один_голос_даёт_разные_профили_у_разных_владельцев` | один голос даёт разные профили у разных владельцев |
| `test_профиль_вы_у_каждого_свой` | профиль вы у каждого свой |
| `test_слияние_чужих_профилей_отбивается` | Иначе чужой профиль можно было бы «слить» в свой и забрать вместе с ним отпечатки голоса и реплики чужих встреч. |
| `test_чужой_отпечаток_не_удаляется` | чужой отпечаток не удаляется |
| `test_чужой_отпечаток_не_участвует_в_сравнении` | Главное следствие разделения: чужие голоса не конкурируют за порог. Один и тот же голос у первого владельца узнаётся как знакомый, у второго — заводит новый профиль, хотя вектор тот же самый. |

### `test_search.py`

Путь: `server/tests/test_search.py`  
Нарезка разговора на куски и устойчивость поиска к чужим размерностям. Векторы считает приложение, здесь их нет вовсе. На сервере осталось то, для чего модель не нужна: нарезка (она про содержимое встречи) и сравнение готовых векторов. Качество поиска проверяется не здесь, а замером на эталоне (scripts/eval_search.py) — тест отвечает «работает/сломано», а не «насколько хорошо».

| Тест | Что проверяет |
|---|---|
| `test_chunk_keeps_replica_whole` | Граница куска проходит по реплике, а не по символу: разрезанная посреди фразы реплика теряет смысл вместе с вектором. |
| `test_chunk_skips_empty_segments` | Пустые реплики в кусок не попадают — иначе в тексте копятся лишние пробелы. |
| `test_chunks_glue_replicas_until_limit` | Короткие реплики склеиваются в кусок: вектор от «Да-да, согласен» — шум. |
| `test_no_chunks_from_silence` | Встреча без единой реплики кусков не даёт и к модели не ходит. |
| `test_уцелевшие_куски_ищутся_несмотря_на_чужие` | Модель перекачали новой версией, пересчёт прошёл наполовину: склейка в матрицу не делится нацело, и вместо пустой выдачи был бы отказ сервера. |
| `test_чужая_размерность_не_роняет_поиск` | Модель сменили, а пересчёт не прошёл целиком: старые векторы трёхмерные, новый запрос пятимерный. Умножение матриц упало бы невнятной ошибкой прямо посреди запроса человека. |

### `test_search_vectors.py`

Путь: `server/tests/test_search_vectors.py`  
Сервер хранит присланные векторы и ищет по ним, но сам их не считает. Эмбеддинги уехали в приложение: у каждого своя модель и свой адрес. Сравнение векторов модели не требует — это скалярное произведение, поэтому поиск остался на сервере: по сети едет вопрос в килобайтах, а не вся матрица в мегабайтах.

| Тест | Что проверяет |
|---|---|
| `test_векторы_чужой_модели_в_выдачу_не_лезут` | векторы чужой модели в выдачу не лезут |
| `test_дальний_вектор_ранжируется_ниже` | дальний вектор ранжируется ниже |
| `test_повторная_индексация_не_плодит_куски` | Иначе одна встреча заняла бы всю выдачу одинаковыми цитатами. |
| `test_принятые_векторы_находятся_поиском` | принятые векторы находятся поиском |
| `test_проиндексированная_встреча_больше_не_ждёт` | проиндексированная встреча больше не ждёт |
| `test_сервер_отдаёт_куски_а_не_векторы` | Нарезка осталась на сервере: она про содержимое встречи, а не про модель. Имя модели при этом обязательно приходит от приложения: сервер больше не знает, чем считают векторы, и «кого индексировать» зависит именно от неё. |
| `test_чужую_встречу_проиндексировать_нельзя` | Иначе чужой архив можно было бы наполнить своими векторами и вытянуть его цитаты собственным поиском. |

### `test_summary_intake.py`

Путь: `server/tests/test_summary_intake.py`  
Сервер принимает протокол, составленный клиентом. Раньше протокол считал сам сервер. Теперь генерация уезжает к человеку — у каждого свой адрес модели, свой ключ и свой лимит, — а серверу остаётся принять результат и проверить, что подписывают свою встречу, а не чужую.

| Тест | Что проверяет |
|---|---|
| `test_идущую_встречу_подписать_нельзя` | идущую встречу подписать нельзя |
| `test_неудача_не_стирает_готовый_протокол` | Неудачная попытка пересоздать не должна оставлять человека без того, что уже было составлено. |
| `test_протокол_сохраняется` | протокол сохраняется |
| `test_пустое_тело_отбивается` | пустое тело отбивается |
| `test_успех_затирает_прошлую_ошибку` | успех затирает прошлую ошибку |
| `test_чужую_встречу_подписать_нельзя` | Без этой проверки любой, у кого есть токен, подписал бы чужой встрече любой текст — а протокол читают те, кого на встрече не было. |

### `test_transcriber_junk.py`

Путь: `server/tests/test_transcriber_junk.py`  
Юнит-тесты фильтрации мусора ASR (asr/transcriber.py `_transcribe_sync`): типовые галлюцинации whisper на тишине отбрасываются. Модель не грузим — подменяем backend, load() видит его и не качает веса.

| Тест | Что проверяет |
|---|---|
| `test_junk_case_and_punctuation_dropped` | Фильтр галлюцинаций не зависит от регистра и знаков препинания. |
| `test_junk_exact_dropped` | Типичная галлюцинация ASR на тишине («Продолжение следует...») выбрасывается. |
| `test_no_parts_is_empty` | Если распознавать нечего — результат пустая строка, а не ошибка. |
| `test_parts_joined_and_empties_dropped` | Куски распознавания склеиваются через пробел, пустые пропускаются. |
| `test_real_text_kept` | Осмысленная речь фильтром не трогается. |

### `test_vad.py`

Путь: `server/tests/test_vad.py`  
Юнит-тесты буферной логики сегментатора (audio/vad.py): нарезка _cut и подрезка памяти _trim_buffer. Silero-модель не грузим — конструируем через __new__ и дёргаем чистые методы напрямую.

| Тест | Что проверяет |
|---|---|
| `test_cut_clamps_to_buffer` | Запрос за границу буфера обрезается по факту имеющихся данных, а не падает. |
| `test_cut_rejects_too_short` | Фрагмент короче порога речи не становится сегментом (щелчки и вздохи отбрасываются). |
| `test_cut_returns_segment_with_timestamps` | У вырезанного сегмента корректные метки времени, а аудио — копия, а не вид на общий буфер. |
| `test_trim_buffer_during_speech_keeps_from_start` | Во время речи буфер подрезается от начала фразы с запасом паддинга — начало реплики не теряется. |
| `test_trim_buffer_idle_keeps_recent` | В тишине буфер не растёт бесконечно: хранятся только последние секунды. |

## Компонентные (API-слой)

*REST через FastAPI TestClient в одном процессе*

### `test_api.py`

Путь: `server/tests/test_api.py`  
Компонентные тесты REST-API: эндпоинты через FastAPI TestClient. Это НЕ функциональные тесты «чёрным ящиком», и важно не выдавать их за таковые: TestClient вызывает приложение внутри процесса (без сети и без живого сервера), а данные для read-эндпоинтов засеваются прямо в БД через crud — то есть в обход публичного интерфейса. Проверяются коды ответов и форма данных. Функциональную проверку — систему целиком глазами пользователя — даёт tests/test_e2e_live.py: настоящий uvicorn, WebSocket, встреча от аудио до REST.

| Тест | Что проверяет |
|---|---|
| `test_asr_state` | /api/asr сообщает текущий движок, модель и список доступных моделей по движкам. |
| `test_delete_meeting` | Встреча удаляется, повторное удаление даёт 404. |
| `test_delete_missing_speaker_404` | Удаление несуществующего спикера даёт 404. |
| `test_delete_missing_voiceprint_404` | Удаление несуществующего отпечатка даёт 404. |
| `test_delete_self_speaker_forbidden` | Профиль владельца «Вы» удалить нельзя — 400. |
| `test_export_md_and_txt` | Выгрузка встречи работает в обоих форматах с правильным Content-Type и содержит транскрипт. |
| `test_export_unknown_meeting_404` | Выгрузка несуществующей встречи даёт 404. |
| `test_health` | /api/health отдаёт статус сервера, состояние ASR, диаризации и выбранного провайдера LLM. |
| `test_meeting_detail_reports_mode` | Карточка встречи отдаёт тип встречи — клиент показывает его чипом. |
| `test_meetings_list_and_get` | Список и карточка встречи отдают сохранённые данные; несуществующий id даёт 404. |
| `test_merge_keeps_richer_profile_and_moves_segments` | Целевым становится профиль с бо́льшим числом реплик (см. registry.merge), реплики второго переезжают к нему, сам он исчезает из списка. |
| `test_merge_needs_two_speakers` | Слияние профилей требует ровно двух id — иначе 400. |
| `test_merge_same_speaker_rejected` | Слить профиль сам с собой нельзя — 400. |
| `test_speaker_rename` | Спикер переименовывается; для несуществующего id — 404. |
| `test_startup_closes_meetings_stuck_in_summarizing` | Сервер убили посреди резюме — при следующем старте встреча не висит. Довести её до конца больше некому: фоновая задача умерла вместе с процессом, а клиент опрашивал бы статус «summarizing» до бесконечности. |
| `test_voiceprint_audio_404_when_absent` | Аудио несуществующего отпечатка даёт 404, а не падение с трейсбеком. |

## Интеграционные (БД)

*миграции схемы на реальном файле SQLite*

### `test_migrations.py`

Путь: `server/tests/test_migrations.py`  
Интеграционные тесты миграций схемы БД (db/database.py) на реальном файле SQLite: старая схема → миграция → апгрейд без потери данных. Быстрые (без моделей), поэтому идут в дефолтном прогоне.

| Тест | Что проверяет |
|---|---|
| `test_chunks_table_appears_in_existing_db` | У базы, заведённой до поиска, таблица кусков появляется сама. Отдельной миграции для неё нет намеренно: create_all добавляет ОТСУТСТВУЮЩИЕ таблицы (в отличие от колонок в существующих). Тест сторожит именно это — если таблицу однажды заменят колонкой в segments, у Сергея и куратора сервер молча останется без поиска. |
| `test_meeting_mode_migration_runs_before_autoincrement` | Регрессия: _migrate_autoincrement строит SELECT по колонкам актуальной модели, поэтому на старой базе без meeting_mode он падает с «no such column» — прямо при старте сервера. Колонку обязана добавить предыдущая миграция. |
| `test_migrate_autoincrement_rebuilds_and_preserves` | Старая таблица без AUTOINCREMENT перестраивается без потери данных (иначе id удалённой встречи мог достаться новой). |
| `test_migrate_meeting_mode_adds_column_and_backfills` | Колонка типа встречи добавляется к старой базе, существующие встречи помечаются как планёрки; повторный запуск безопасен. |
| `test_migrate_meeting_owner_adds_column` | Колонка владельца добавляется к старой базе, встречи остаются ничейными. Ничейными намеренно: людей на личном сервере нет, приписывать записи некому. Достанутся они первому заведённому человеку (см. auth.create_user) — иначе он закрыл бы сервер токеном и обнаружил пустой архив. |
| `test_migrate_samples_adds_columns_and_merges` | Аудио-образцы сливаются с отпечатками в одно «звучание», лишние файлы удаляются. |
| `test_migrate_speaker_owner_adds_column` | Колонка владельца добавляется к таблице голосов; профили остаются ничейными и достаются первому заведённому человеку вместе со встречами. Порядок тот же, что у встреч: строго ДО перестройки таблиц — speakers там тоже пересобирается по списку колонок из модели. |
| `test_migrate_voiceprints_moves_centroid` | Центроид голоса переезжает из строки спикера в отдельную таблицу отпечатков. |

## Интеграционные (модели)

*реальная ECAPA на синтезированных голосах*

### `test_diarization_integration.py`

Путь: `server/tests/test_diarization_integration.py`  
Интеграционный тест: реальная ECAPA + три синтезированных голоса macOS. Запуск (небыстрый, грузит модель): .venv/bin/python -m pytest -m integration Проверяет всю цепочку «звук → эмбеддинг → профиль» на трёх разных голосах. Синтетические голоса стабильнее человеческих, поэтому это дымовой тест пайплайна; для оценки живых голосов есть scripts/eval_voices.py.

| Тест | Что проверяет |
|---|---|
| `test_current_threshold_separates_three_voices` | С порогом из конфига три голоса дают ровно три профиля, без раздвоений и склеек. |
| `test_own_phrases_closer_than_foreign` | Худшая близость «своих» фраз должна быть выше лучшей «чужой» — иначе никакой порог не разделит эти голоса. |
| `test_speaker_echo_yields_single_segment` | Эхо из колонок: голос из звонка играет в комнате и попадает в микрофон с задержкой. В смешанном потоке это ОДИН сегмент (копия совпадает по времени с оригиналом), и доминанта — системный канал. |

## E2E (системные)

*живой uvicorn + WebSocket: встреча целиком, как у пользователя*

### `test_e2e_live.py`

Путь: `server/tests/test_e2e_live.py`  
E2E: одноразовый живой сервер + WebSocket-клиент, как настоящее приложение. Поднимает uvicorn на :8766 с временной папкой данных (модели — симлинком из server/data/models, чтобы не перекачивать веса; БД и голоса одноразовые — живая база не затрагивается). Прогоняет встречи целиком: синтезированный звук → микшер → VAD → ASR → диаризация → события → REST. Протокол сюда не входит: его считает приложение, и сквозной прогон обеих половин живёт в клиенте (client/src/e2e). Здесь — конвейер звука целиком. Запуск (медленный, несколько минут): .venv/bin/python -m pytest -m e2e Тесты зависят друг от друга по данным и выполняются по порядку — база накапливает спикеров и встречи, как при реальном использовании.

| Тест | Что проверяет |
|---|---|
| `test_01_speaker_echo_no_duplicates` | Звонок на колонках: голос собеседника и его эхо в микрофоне → один сегмент, приписан НЕ «Вы», канал — system. |
| `test_02_self_enrolls_and_is_remembered` | Голос из микрофона становится «Вы»; на следующей встрече тот же голос узнаётся даже из системного канала — база голосов живёт между встречами. |
| `test_03_two_voices_and_short_reply` | Два голоса в одной встрече → два разных профиля (Милена — та же, что во встрече №1, владелец узнан по голосу); короткая реплика (<0.4 с) приклеивается к последнему говорившему. |
| `test_04_guest_gets_new_profile` | Незнакомый голос получает новый профиль, не примазываясь ни к «Вы», ни к Милене. |
| `test_05_silent_meeting` | Тишина всю встречу: ноль сегментов, встреча всё равно завершается. Протокола нет и ошибки нет: считать его теперь некому — это делает приложение, и «встреча без речи» оно объясняет само (см. сквозной прогон клиента, client/src/e2e). Серверу важно другое: не оставить встречу висеть. |
| `test_06_long_monologue_is_split` | Монолог длиннее лимита сегмента (15 с) режется на части, стоп дожимает хвост, встреча завершается. |
| `test_07_ws_abort_finalizes_meeting` | Клиент оборвался без stop (закрыли крышку ноутбука) — встреча всё равно завершается на сервере и не остаётся вечно live. |
| `test_08_meeting_never_hangs_in_summarizing` | Встреча не остаётся в «составляется»: сервер протоколов не считает. Раньше здесь проверялась честная ошибка резюме — её писал сервер, потому что считал он же. Теперь считает приложение, и «модель не ответила» — его забота; серверу нельзя другое: оставить встречу в состоянии, из которого её никто не выведет, и человек ждал бы вечно. |
| `test_09_rest_operations` | Переименование, merge, экспорт, удаление — поверх накопленных данных. |
| `test_10_asr_switch_rules` | Переключение модели: 409 во время live-встречи, 400 на невалидную пару, выбор персистится в asr.json. |

## Нагрузочные

*параллельные встречи, задержка и память*

### `test_load_smoke.py`

Путь: `server/tests/test_load_smoke.py`  
Дымовой нагрузочный тест (-m load, исключён из дефолтного прогона): две параллельные встречи проходят целиком без ошибок. Полноценный прогон — скриптом scripts/loadtest.py. Требует кэш моделей server/data/models.

| Тест | Что проверяет |
|---|---|
| `test_two_concurrent_meetings_survive` | Две одновременные встречи проходят целиком без ошибок, память измерима — дымовая проверка перед полноценной нагрузкой. |

## Клиент (Vitest)

*чистая логика фронтенда в jsdom, без браузера*

### `live.test.ts`

Путь: `client/src/api/live.test.ts`  
LiveClient.sendAudio; LiveClient.start; LiveClient: токен первым кадром

| Тест | Что проверяет |
|---|---|
| `ставит байт канала первым и копирует PCM следом` | ставит байт канала первым и копирует PCM следом |
| `молчит, если сокет не открыт` | молчит, если сокет не открыт |
| `передаёт тип встречи серверу (от него зависят промпты)` | передаёт тип встречи серверу (от него зависят промпты) |
| `шлёт auth сразу после открытия, до всего остального` | шлёт auth сразу после открытия, до всего остального |
| `шлёт кадр и с пустым токеном: отказ должен прийти сразу, а не по таймауту` | шлёт кадр и с пустым токеном: отказ должен прийти сразу, а не по таймауту |

### `rest.test.ts`

Путь: `client/src/api/rest.test.ts`  
rest.request; rest.request: токен доступа

| Тест | Что проверяет |
|---|---|
| `возвращает разобранный JSON при успехе` | возвращает разобранный JSON при успехе |
| `бросает body.detail при ошибке` | бросает body.detail при ошибке |
| `падает на` | падает на |
| `не шлёт заголовок, пока токен не задан` | не шлёт заголовок, пока токен не задан |
| `шлёт токен схемой Bearer` | шлёт токен схемой Bearer |
| `не теряет тип содержимого у запросов со своим телом` | не теряет тип содержимого у запросов со своим телом |

### `capture.test.ts`

Путь: `client/src/audio/capture.test.ts`  
looksLikeLoopback

| Тест | Что проверяет |
|---|---|
| `узнаёт виртуальные loopback-драйверы` | узнаёт виртуальные loopback-драйверы |
| `не путает обычные устройства с loopback` | не путает обычные устройства с loopback |

### `Transcript.test.ts`

Путь: `client/src/components/Transcript.test.ts`  
formatTime; groupSegments; renameInSegments; applyMergeToSegments

| Тест | Что проверяет |
|---|---|
| `форматирует секунды меньше минуты` | форматирует секунды меньше минуты |
| `форматирует минуты:секунды` | форматирует минуты:секунды |
| `добавляет часы начиная с 3600 с` | добавляет часы начиная с 3600 с |
| `зажимает отрицательные к нулю и отбрасывает дробь` | зажимает отрицательные к нулю и отбрасывает дробь |
| `склеивает подряд идущие реплики одного спикера` | склеивает подряд идущие реплики одного спикера |
| `рвёт группу на смене спикера` | рвёт группу на смене спикера |
| `рвёт группу после паузы длиннее 30 секунд` | рвёт группу после паузы длиннее 30 секунд |
| `не считает неизвестных спикеров одним человеком по ошибке` | не считает неизвестных спикеров одним человеком по ошибке |
| `пустой список даёт пустой результат` | пустой список даёт пустой результат |
| `меняет имя во всех репликах спикера, а не только в одной` | меняет имя во всех репликах спикера, а не только в одной |
| `не трогает реплики без спикера` | не трогает реплики без спикера |
| `не мутирует исходный массив` | не мутирует исходный массив |
| `реплики исчезнувшего профиля переезжают на целевой` | реплики исчезнувшего профиля переезжают на целевой |
| `целевой профиль тоже переименовывается, если сервер взял имя источника` | целевой профиль тоже переименовывается, если сервер взял имя источника |
| `не трогает реплики без спикера и не мутирует исходный массив` | не трогает реплики без спикера и не мутирует исходный массив |

### `meeting.e2e.test.ts`

Путь: `client/src/e2e/meeting.e2e.test.ts`  

| Тест | Что проверяет |
|---|---|
| `транскрипт приходит с сервера, протокол уходит на сервер` | транскрипт приходит с сервера, протокол уходит на сервер |

### `hints.test.ts`

Путь: `client/src/llm/hints.test.ts`  
когда дёргать модель; что уходит модели; что делать с ответом; ошибки связи; вопрос участника; швы, которые не ловились по отдельности

| Тест | Что проверяет |
|---|---|
| `молчит, пока разговора мало` | молчит, пока разговора мало |
| `не частит: между подсказками выдерживается пауза` | не частит: между подсказками выдерживается пауза |
| `молчание модели удлиняет паузу, а серия молчаний — сильнее` | молчание модели удлиняет паузу, а серия молчаний — сильнее |
| `новое отделено от контекста, свои подсказки вплетены` | новое отделено от контекста, свои подсказки вплетены |
| `по кнопке разговор показывается целиком, без деления` | по кнопке разговор показывается целиком, без деления |
| `почти-дубль недавней подсказки человеку не показывается` | почти-дубль недавней подсказки человеку не показывается |
| `по кнопке дубль показывается: человек попросил явно` | по кнопке дубль показывается: человек попросил явно |
| `по кнопке молчание модели объясняется, а не пропадает` | по кнопке молчание модели объясняется, а не пропадает |
| `один сбой не выключает подсказки, но откладывает следующую` | один сбой не выключает подсказки, но откладывает следующую |
| `после пяти сбоев подряд подсказки гаснут с объяснением` | после пяти сбоев подряд подсказки гаснут с объяснением |
| `выделенные реплики уходят отдельным блоком` | выделенные реплики уходят отдельным блоком |
| `пустой ответ модели — ошибка, а не пустая реплика в чате` | пустой ответ модели — ошибка, а не пустая реплика в чате |
| `одной подсказке достаётся доля тарифа, а не весь минутный лимит` | одной подсказке достаётся доля тарифа, а не весь минутный лимит |
| `окно разговора считается от доли, а не от всего лимита` | окно разговора считается от доли, а не от всего лимита |
| `кнопка отменяет фоновую подсказку, а не показывает обе` | кнопка отменяет фоновую подсказку, а не показывает обе |

### `ollama.test.ts`

Путь: `client/src/llm/ollama.test.ts`  
OllamaClient.generate; OllamaClient.embed; OllamaClient.models

| Тест | Что проверяет |
|---|---|
| `отдаёт текст ответа и не шлёт num_predict` | отдаёт текст ответа и не шлёт num_predict |
| `вырезает размышления, если модель вывалила их в текст` | вырезает размышления, если модель вывалила их в текст |
| `режет хвостовой слэш в адресе, а не склеивает двойной` | режет хвостовой слэш в адресе, а не склеивает двойной |
| `на недоступный адрес объясняет, что делать` | на недоступный адрес объясняет, что делать |
| `на 404 подсказывает скачать именно эту модель` | на 404 подсказывает скачать именно эту модель |
| `шлёт всю пачку одним запросом` | шлёт всю пачку одним запросом |
| `пустой список не ходит в сеть` | пустой список не ходит в сеть |
| `замечает, что модель не для эмбеддингов` | замечает, что модель не для эмбеддингов |
| `возвращает имена скачанных моделей` | возвращает имена скачанных моделей |
| `недоступный адрес — пустой список, а не исключение` | недоступный адрес — пустой список, а не исключение |

### `openai.test.ts`

Путь: `client/src/llm/openai.test.ts`  
parseReset; OpenAiClient.generate; OpenAiClient.generate потоком; OpenAiClient.models; OpenAiClient.tokenLimit; OpenAiClient.waitForBudget; ожидание лимита перед запросом

| Тест | Что проверяет |
|---|---|
| `шлёт ключ заголовком и возвращает текст` | шлёт ключ заголовком и возвращает текст |
| `системный промпт уходит отдельным сообщением` | системный промпт уходит отдельным сообщением |
| `причину отказа берёт из error.message, а не из кода` | причину отказа берёт из error.message, а не из кода |
| `недоступный адрес объясняет словами` | недоступный адрес объясняет словами |
| `разделяет мысли и ответ, склеивает текст` | разделяет мысли и ответ, склеивает текст |
| `служебные строки потока не ломают разбор` | служебные строки потока не ломают разбор |
| `отсеивает не-текстовые и слишком короткий контекст` | отсеивает не-текстовые и слишком короткий контекст |
| `берёт лимит из заголовка ответа` | берёт лимит из заголовка ответа |
| `сетевая ошибка не ломает сохранение настроек` | сетевая ошибка не ломает сохранение настроек |
| `ждёт ровно столько, сколько обещал провайдер, плюс секунду` | ждёт ровно столько, сколько обещал провайдер, плюс секунду |
| `влезающий запрос не ждёт вовсе` | влезающий запрос не ждёт вовсе |
| `запрос дожидается восстановления окна, а не получает отказ` | запрос дожидается восстановления окна, а не получает отказ |
| `ждём ровно то, что обещал провайдер` | ждём ровно то, что обещал провайдер |

### `port.test.ts`

Путь: `client/src/llm/prompts/port.test.ts`  
промпт протокола совпадает с серверным; сборка транскрипта совпадает с серверной; нарезка по репликам совпадает с серверной; нарезка не рвёт реплики; промпт подсказки совпадает с серверным; промпт ответа участнику совпадает с серверным; разбор ответа модели совпадает с серверным; промпт ответа по прошлым встречам совпадает с серверным

| Тест | Что проверяет |
|---|---|
| `реплика длиннее предела уходит в свой кусок целиком` | реплика длиннее предела уходит в свой кусок целиком |
| `предел 0 — не режем вовсе (выгрузка полного транскрипта)` | предел 0 — не режем вовсе (выгрузка полного транскрипта) |
| `вопрос по выделенным репликам сформулирован так же` | вопрос по выделенным репликам сформулирован так же |

### `search.test.ts`

Путь: `client/src/llm/search.test.ts`  
индексация; поиск; ответ по найденному

| Тест | Что проверяет |
|---|---|
| `считает векторы своей моделью и возвращает их вместе с кусками` | считает векторы своей моделью и возвращает их вместе с кусками |
| `нечего считать — к модели не ходим вовсе` | нечего считать — к модели не ходим вовсе |
| `вектор вопроса считает клиент, сравнение просит у сервера` | вектор вопроса считает клиент, сравнение просит у сервера |
| `пустой вопрос не будит ни модель, ни сервер` | пустой вопрос не будит ни модель, ни сервер |
| `отвечает моделью протокола по показанным фрагментам` | отвечает моделью протокола по показанным фрагментам |
| `ничего не нашлось — к модели не ходим и не выдумываем ответ` | ничего не нашлось — к модели не ходим и не выдумываем ответ |

### `settings.test.ts`

Путь: `client/src/llm/settings.test.ts`  
настройки моделей у клиента; llmReady

| Тест | Что проверяет |
|---|---|
| `без сохранённого — локальная модель на этой же машине` | без сохранённого — локальная модель на этой же машине |
| `сохранённое возвращается как есть` | сохранённое возвращается как есть |
| `частичное сохранение не стирает остальное` | частичное сохранение не стирает остальное |
| `мусор в лимитах не ломает настройки` | мусор в лимитах не ломает настройки |
| `нечисловые лимиты отбрасываются` | нечисловые лимиты отбрасываются |
| `локальная модель готова с адресом и именем` | локальная модель готова с адресом и именем |
| `локальная без адреса — не готова` | локальная без адреса — не готова |
| `внешний API без ключа — не готов` | внешний API без ключа — не готов |
| `внешний API со всем нужным — готов` | внешний API со всем нужным — готов |

### `similarity.test.ts`

Путь: `client/src/llm/similarity.test.ts`  
похожесть строк совпадает с difflib; свойства меры

| Тест | Что проверяет |
|---|---|
| `одинаковые строки — единица` | одинаковые строки — единица |
| `порог 0.85 ловит перефразировку и пропускает разное` | порог 0.85 ловит перефразировку и пропускает разное |

### `summary.test.ts`

Путь: `client/src/llm/summary.test.ts`  
протокол одним запросом; длинная встреча по фрагментам; модель не уместила ответ

| Тест | Что проверяет |
|---|---|
| `короткая встреча — один вызов модели` | короткая встреча — один вызов модели |
| `встреча без речи — понятная причина, а не пустой протокол` | встреча без речи — понятная причина, а не пустой протокол |
| `режет, считает заметки и сводит их отдельным запросом` | режет, считает заметки и сводит их отдельным запросом |
| `тариф меньше самого промпта — честный отказ вместо крошева` | тариф меньше самого промпта — честный отказ вместо крошева |
| `делит фрагмент пополам и пробует снова` | делит фрагмент пополам и пробует снова |
| `чужая ошибка пробрасывается, а не дробится бесконечно` | чужая ошибка пробрасывается, а не дробится бесконечно |

### `store.test.ts`

Путь: `client/src/store.test.ts`  
store

| Тест | Что проверяет |
|---|---|
| `getServerUrl падает на дефолт без сохранённого адреса` | getServerUrl падает на дефолт без сохранённого адреса |
| `getServerUrl срезает завершающие слеши` | getServerUrl срезает завершающие слеши |
| `getSetting/setSetting работают через префикс stenograf.` | getSetting/setSetting работают через префикс stenograf. |
| `isDebugMode отражает флаг` | isDebugMode отражает флаг |

## Чего здесь нет

UI-страницы (`LivePage`, `SettingsPage`, `SpeakersPage`) юнит-тестами не покрыты — они проверяются `tsc --noEmit`, сборкой `vite build` и ручным чек-листом [TESTING.md](TESTING.md). Там же то, что автоматика проверить не может в принципе: реальный микрофон, эхо колонок, упакованное приложение.

