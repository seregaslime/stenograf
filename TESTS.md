# Каталог тестов — Стенограф

> ⚙️ **Сгенерирован автоматически** из кода тестов (`python scripts/test_inventory.py`), руками не редактируется.
> Обновлён: 09.08.2026 18:24

Всего тестовых функций на сервере: **238** (с учётом параметризации — 258 проверок). Клиент: **18**.

Здесь написано, **что именно** проверяет каждый тест. Покрытие кода (сколько строк исполнилось) — отдельная метрика, она в [TEST_REPORT.md](TEST_REPORT.md).

## Оглавление

- **Юнит** — 192 тестов
  - [`test_answer.py`](#test-answerpy) —  (7)
  - [`test_bench_registry.py`](#test-bench-registrypy) —  (5)
  - [`test_config.py`](#test-configpy) — конфигурация и персист выбора движка ASR (2)
  - [`test_crud.py`](#test-crudpy) — операции с БД: встречи, спикеры, сегменты (8)
  - [`test_hints.py`](#test-hintspy) — движок подсказок: триггер, дедуп, бэкофф, SKIP, кнопка «подсказать сейчас» (31)
  - [`test_live_session.py`](#test-live-sessionpy) — живая сессия: разрез сегментов по смене говорящего (5)
  - [`test_llm_provider.py`](#test-llm-providerpy) — клиент OpenAI-совместимого API, роутер local↔api, бюджет контекста (43)
  - [`test_loadtest_metrics.py`](#test-loadtest-metricspy) —  (7)
  - [`test_mixer.py`](#test-mixerpy) — микшер каналов: склейка mic/system и определение доминанты (10)
  - [`test_ollama_client.py`](#test-ollama-clientpy) — клиент локальной Ollama: запросы и обработка ошибок (6)
  - [`test_prompts.py`](#test-promptspy) — сборка промптов: режимы встречи, право промолчать, разбор ответа (18)
  - [`test_registry.py`](#test-registrypy) — диаризация: сопоставление голосов, приоры, слияние профилей (18)
  - [`test_summary_format.py`](#test-summary-formatpy) — форматирование транскрипта для протокола и лимиты длины (7)
  - [`test_summary_generate.py`](#test-summary-generatepy) —  (15)
  - [`test_transcriber_junk.py`](#test-transcriber-junkpy) — фильтр галлюцинаций ASR на тишине (5)
  - [`test_vad.py`](#test-vadpy) — нарезка речи по паузам, отбрасывание коротких фрагментов (5)
- **Компонентные (API-слой)** — 27 тестов
  - [`test_api.py`](#test-apipy) — REST-эндпоинты: коды ответов, форма данных, отказы (27)
- **Интеграционные (БД)** — 5 тестов
  - [`test_migrations.py`](#test-migrationspy) — апгрейд старых баз без потери данных (5)
- **Интеграционные (модели)** — 3 тестов
  - [`test_diarization_integration.py`](#test-diarization-integrationpy) — реальная модель ECAPA на синтезе голосов macOS (3)
- **E2E (системные)** — 10 тестов
  - [`test_e2e_live.py`](#test-e2e-livepy) — сквозные сценарии встречи через WebSocket (10)
- **Нагрузочные** — 1 тестов
  - [`test_load_smoke.py`](#test-load-smokepy) — дымовой нагрузочный прогон (1)
- **Клиент (Vitest)** — 18 тестов
  - [`live.test.ts`](#livetestts)
  - [`rest.test.ts`](#resttestts)
  - [`capture.test.ts`](#capturetestts)
  - [`Transcript.test.ts`](#Transcripttestts)
  - [`store.test.ts`](#storetestts)

## Юнит

*функция или класс в изоляции, зависимости замоканы*

### `test_answer.py`

Путь: `server/tests/test_answer.py`  
Ответ на вопрос участника из окна чата (LiveSession._answer). Отличается от подсказки принципиально: тему задаёт человек, ответ обязателен, молчать нельзя. Проверяем сборку промпта, фильтр выделенных реплик по встрече и поведение при ошибках. Без сети и без реального LLM.

| Тест | Что проверяет |
|---|---|
| `test_empty_question_without_selection_is_rejected` | Пустой вопрос без выделения — спрашивать не о чем, LLM не зовём. |
| `test_garbage_ids_do_not_crash` | Клиент может прислать что угодно — до запроса в БД доходят только числа. |
| `test_llm_error_reaches_the_user` | Ошибка связи показывается сразу: человек спросил и ждёт ответа. Бэкофф фоновых подсказок здесь намеренно не участвует — глушить явные вопросы из-за неудач автоматического цикла нельзя. |
| `test_question_without_selection_still_works` | Спросить можно и без выделения — тогда контекстом идёт весь разговор. |
| `test_quoted_segments_go_into_the_prompt` | Выделенные реплики попадают в промпт отдельным блоком «вопрос про них». |
| `test_second_question_while_answering_is_refused` | Пока модель отвечает, второй вопрос не запускаем — иначе два запроса в лимит токенов в минуту вместо одного. |
| `test_segments_from_another_meeting_are_ignored` | id приходят от клиента — чужая встреча через них вытянуться не должна. Без фильтра по meeting_id в crud.segments_by_ids произвольные числа отдали бы текст постороннего разговора прямо в промпт. |

### `test_bench_registry.py`

Путь: `server/tests/test_bench_registry.py`  
Бенчмарк роста библиотеки голосов (scripts/bench_registry.py). Тесты не меряют производительность (это дело самого бенчмарка), а проверяют, что он меряет ТО, что заявляет: полный перебор отпечатков без обращения к БД. Иначе бенчмарк тихо перестанет отражать реальность, и вывод «диаризация не узкое место» окажется основан на сломанном замере.

| Тест | Что проверяет |
|---|---|
| `test_embeddings_are_unit_vectors` | match_all считает скалярное произведение как косинусную близость — значит векторы обязаны быть нормированы, иначе пороги теряют смысл. |
| `test_fill_creates_requested_library` | fill creates requested library |
| `test_measure_does_not_touch_real_db` | Заглушка БД обязана перехватывать все обращения: если бенчмарк однажды начнёт писать в рабочую базу, это заметят слишком поздно. |
| `test_measure_reports_linear_growth` | Главная проверка: время растёт пропорционально размеру библиотеки. Если кто-то заменит перебор на индекс, тест упадёт — и это будет правильный сигнал обновить выводы в LOAD_REPORT.md, а не молча тащить старые. |
| `test_probe_matches_existing_speaker` | Проба должна УЗНАВАТЬСЯ: со случайным вектором match_all пошла бы заводить нового спикера, и замер мерил бы не перебор, а запись в БД. |

### `test_config.py`

Путь: `server/tests/test_config.py`  
Юнит-тесты конфигурации (config.py): дефолты и персист выбора ASR-движка.

| Тест | Что проверяет |
|---|---|
| `test_defaults` | Дефолты конфига соответствуют заявленным в README (движок ASR, провайдер LLM, пути к данным). |
| `test_save_and_load_asr_choice` | Выбор движка и модели ASR переживает перезапуск: пишется в asr.json и читается обратно. |

### `test_crud.py`

Путь: `server/tests/test_crud.py`  
Юнит-тесты операций с БД (db/crud.py) на in-memory SQLite (фикстура db_session).

| Тест | Что проверяет |
|---|---|
| `test_add_segment_and_list_meetings` | Список встреч отдаёт название, число реплик и признак наличия резюме. |
| `test_create_and_end_meeting` | Встреча создаётся в статусе live с фолбэком названия; завершение проставляет время один раз и повторно его не сдвигает. |
| `test_create_speaker_default_name` | Новому спикеру даётся имя-заглушка «Спикер N» и он не помечается владельцем. |
| `test_list_speakers_counts_and_order` | Владелец «Вы» идёт первым в списке; у каждого спикера считаются реплики, встречи и число отпечатков голоса. |
| `test_meeting_segments_ordered_and_dict` | Реплики возвращаются по возрастанию времени; в словаре близость округлена до 3 знаков, а спикер может быть None. |
| `test_reassign_segments` | Перенос реплик между спикерами возвращает число перенесённых; повторный перенос уже ничего не двигает. |
| `test_rename_speaker` | Переименование обрезает пробелы по краям и сохраняется в БД; для несуществующего id возвращается None. |
| `test_self_speaker_created_once` | Профиль владельца «Вы» создаётся один раз: повторный вызов возвращает тот же id, а не плодит дубли. |

### `test_hints.py`

Путь: `server/tests/test_hints.py`  
Движок подсказок во время встречи: чистые функции (дедуп, решение триггера) и поведение _emit_hint (отправка, дедуп, бэкофф при ошибках, «подсказать сейчас»). Без сети и без реального LLM — через фейковый роутер.

| Тест | Что проверяет |
|---|---|
| `test_backchannel_does_not_trigger` | Поддакивания подсказку не запускают — иначе модель дёргалась бы на «угу». |
| `test_boundary_survives_deque_overflow` | Дека реплик переполняется и теряет старое слева. Счётчик монотонный, поэтому граница не съезжает — сохранённый индекс указывал бы не туда. |
| `test_emit_hint_disables_after_max_fails` | После серии ошибок подряд подсказки отключаются, чтобы не долбить недоступный сервис. |
| `test_emit_hint_error_is_not_permanent` | Одна ошибка LLM не выключает подсказки: наращивается бэкофф и шлётся одно уведомление. |
| `test_emit_hint_sends_then_dedups` | Первая подсказка доходит до клиента, повторная с тем же текстом отсеивается дедупом. |
| `test_force_bypasses_dedup` | По кнопке подсказка доставляется, даже если совпадает с предыдущей. |
| `test_force_keeps_hint_markers` | «Смотреть на весь разговор» не значит «забыть свои ответы». Первая реализация на force выбрасывала пометки о выданных подсказках, и модель снова видела закрытые вопросы открытыми — ровно та проблема, ради которой границу и делали. |
| `test_force_looks_at_whole_conversation` | Кнопка «Подсказать сейчас» игнорирует границу: человек попросил явно, значит смотрим на разговор целиком, даже если нового ничего не было. |
| `test_force_prompt_has_no_skip_rule` | В промпте по кнопке слова SKIP нет вовсе — на явный запрос модель молчать не вправе. |
| `test_force_reports_instead_of_staying_silent` | Раньше кнопка молча ничего не делала — теперь всегда отвечает. |
| `test_gap_still_limits_rate` | Порог символов НЕ управляет частотой — её держит пауза. Иначе низкий порог означал бы запрос на каждую реплику. |
| `test_hint_now_forces_even_when_disabled` | Кнопка «Подсказать сейчас» срабатывает даже при выключенных подсказках. |
| `test_is_duplicate_catches_near_repeat` | Почти дословный повтор недавней подсказки считается дублем (регистр и знаки не важны), а другая по смыслу — нет. |
| `test_is_duplicate_empty_memory` | При пустой памяти подсказок дублей быть не может. |
| `test_llm_error_keeps_the_boundary` | При сбое связи модель текста не видела — граница обязана остаться, иначе реплики молча выпадут из рассмотрения навсегда. |
| `test_prompt_carries_meeting_context` | В промпт уходят название встречи, участники и режим — без этого модель не понимает темы. |
| `test_real_hint_resets_skip_streak` | Первая же реальная подсказка обнуляет счётчик молчаний и возвращает обычную паузу. |
| `test_second_hint_reacts_only_to_new_text` | Регрессия на живой сценарий: задал вопрос — получил ответ, задал второй — получил ответы на ОБА. Модель не знала, что первый уже закрыт, потому что её собственные подсказки в транскрипт не попадают. |
| `test_short_question_reaches_the_trigger` | Регрессия на живой сценарий: человек задаёт короткий вопрос и замолкает, ожидая подсказку. При старом пороге 200 символов счётчик вставал на 16 и подсказка не приходила никогда — самый ценный повод оказывался недостижим. |
| `test_should_hint_needs_enough_new_text` | Подсказка не запускается, пока не накопилось достаточно нового разговора. |
| `test_should_hint_respects_backoff` | В период бэкоффа после ошибок LLM подсказки не запрашиваются. |
| `test_should_hint_respects_min_gap` | Между подсказками выдерживается минимальный интервал, даже если текста много. |
| `test_skip_does_not_spam_llm` | Сразу после SKIP новый запрос не уходит: нужна свежая порция текста. |
| `test_skip_is_not_sent_to_client` | Модель промолчала — клиент не должен увидеть ничего. |
| `test_skip_keeps_context_and_error_state` | SKIP — не ошибка: бэкофф не трогается, история реплик остаётся, подсказки не выключаются даже после длинной серии молчания. |
| `test_skip_moves_the_boundary` | Модель промолчала — значит текст посмотрела. Не сдвинуть границу означало бы гонять отвергнутый фрагмент по кругу, пока она не надумает подсказку. |
| `test_skip_shortens_then_grows_the_gap` | После молчания пауза короче обычной, но серия молчаний наращивает её до потолка. |
| `test_window_follows_provider_budget` | Окно режется бюджетом провайдера, а не размером деки. |
| `test_window_ignores_budget_without_tariff_limit` | У локальной модели тарифного лимита нет — режем по символам, как раньше. |
| `test_window_never_drops_to_zero` | Лимит меньше самого промпта — отправляем минимум, а не пустоту. Подсказки на таком тарифе невозможны, и провайдер ответит 413 с внятным текстом. Это лучше, чем молча слать запросы без единого слова разговора. |
| `test_window_shrinks_by_prompt_size` | Из тарифного бюджета вычитается сам промпт. Обрезав разговор ровно по бюджету, мы отправили бы его ВМЕСТЕ с инструкциями и всё равно упёрлись бы в лимит. Инструкции стоят около 1100 токенов — больше половины бюджета на бесплатном тарифе. |

### `test_live_session.py`

Путь: `server/tests/test_live_session.py`  
Тесты правил LiveSession, влияющих на то, кому припишется реплика: короткий сегмент («да», «угу») без эмбеддинга приписывается последнему говорившему, но только в течение 4 секунд и только из того же канала; сегмент со сменой доминанты канала режется на части.

| Тест | Что проверяет |
|---|---|
| `test_short_segment_after_long_pause_is_embedded` | Пауза больше 4 с — «прилипание» к последнему спикеру не действует. |
| `test_short_segment_from_other_channel_is_embedded` | Быстрое «да» из звонка сразу после фразы владельца — другой человек: наследование не действует, эмбеддинг считается. |
| `test_short_segment_inherits_self` | Короткая реплика после фразы владельца наследует и флаг «Вы». |
| `test_short_segment_reuses_last_speaker` | Короткое «угу» сразу после реплики — тот же человек, эмбеддинг не считается. |
| `test_split_by_dominance_cuts_segment` | Сегмент со сменой канала посередине режется на две части: аудио не теряется, граница — в точке смены доминанты. |

### `test_llm_provider.py`

Путь: `server/tests/test_llm_provider.py`  
Провайдер LLM: OpenAI-совместимый клиент, роутер local↔api и персист выбора. HTTP мокается httpx.MockTransport — без сети и без внешних зависимостей. Асинхронные вызовы гоняются через asyncio.run (pytest-asyncio в проекте нет).

| Тест | Что проверяет |
|---|---|
| `test_budget_switches_with_provider` | Бюджет — свойство, а не поле: провайдера меняют посреди встречи, и глубина подсказок/резюме обязана поменяться сразу. |
| `test_client_does_not_wait_while_budget_is_free` | Лимит свободен — не спим: фиксированная пауза тратила минуты впустую. |
| `test_client_waits_when_budget_is_spent` | Ждём столько, сколько сказал провайдер, вместо фиксированной паузы. Спать минуту оказалось мало: окно скользящее, и после крупного запроса за 60 секунд восстанавливается не всё — на длинной встрече первый фрагмент проходил, а дальше всё хуже. |
| `test_hints_budget_derived_from_measured_limit` | Бюджет подсказки = (лимит − резерв под ответ) ÷ запросов в минуту. Частота ограничена сверху hints_min_gap_s, поэтому сумма за минуту не превысит лимит по построению — считать расход в реальном времени не нужно. Резерв вычитается потому, что провайдер засчитывает в лимит и место, отведённое модели на ответ. |
| `test_hints_budget_falls_back_when_limit_unknown` | Лимит не измерен — берём запасное значение, а не считаем «без лимита». |
| `test_load_llm_choice_falls_back_when_api_unconfigured` | Если сохранён api, но конфигурация подключения пропала, сервер честно откатывается на локальную модель. |
| `test_local_provider_has_no_token_budget` | У локальной модели тарифного лимита нет — режем по символам, как раньше. |
| `test_max_tokens_is_not_set_by_default` | Предел ответа модели не ограничиваем — списание от этого не уменьшится. Провайдер списывает с минутного лимита фиксированный резерв (2500) независимо от max_tokens, а сама модель способна на 65536. Выставив max_tokens = 2500, мы списание не уменьшаем, зато у рассуждающей модели рассуждения съедают весь отведённый предел и текст ответа приходит пустым. |
| `test_min_context_threshold_is_configurable` | Порог берётся из настроек: поднимаем — модель выпадает из списка. |
| `test_no_tpm_limit_means_no_reserve_subtraction` | Без тарифного лимита вычитать резерв не из чего — бюджет остаётся нулевым. |
| `test_normal_answer_passes_through` | Обычный ответ (finish_reason=stop) проходит как раньше. |
| `test_only_groq_host_is_supported` | only groq host is supported |
| `test_openai_bad_shape_raises_llmerror` | Ответ неожиданной формы не роняет сервер, а даёт внятную ошибку. |
| `test_openai_broken_connection_raises_llmerror` | Обрыв связи на середине ответа — тоже LlmError, а не сырое исключение. |
| `test_openai_connect_error_raises_llmerror` | Недоступный адрес API даёт понятное сообщение с указанием, что проверить. |
| `test_openai_generate_builds_request_and_parses` | Запрос к OpenAI-совместимому API уходит на /chat/completions с моделью, ролями и температурой; ответ разбирается и обрезается. |
| `test_openai_generate_strips_think` | Блок <think> вырезается и из ответа внешнего API (там тоже может стоять qwen3). |
| `test_openai_http_errors_raise_llmerror` | Коды 401/403/404/500 превращаются в понятную пользователю ошибку, а не в сырое исключение. _(×4 кейсов)_ |
| `test_openai_rate_limit_explains_what_to_do` | Тарифный лимит — не поломка, и сообщение должно это отражать. Groq на бесплатном тарифе отвечает 413, когда один запрос крупнее лимита токенов в минуту. Пользователю нужен не код ошибки, а причина (сколько просили и сколько можно) и что с этим делать. _(×2 кейсов)_ |
| `test_openai_requires_base_url_and_model` | Без адреса API или без имени модели запрос не отправляется. |
| `test_openai_timeout_raises_llmerror` | Таймаут превращается в LlmError, а не улетает наружу. Раньше ловился только ConnectError, поэтому упавший посреди запроса VPN убивал фоновую задачу резюме и встреча навсегда оставалась «summarizing». |
| `test_reasoning_effort_can_be_switched_off` | Пустая строка — не передавать параметр вовсе. |
| `test_reasoning_effort_is_sent` | Просим модель думать поменьше: мысли считаются в лимит, а не показываются. Замер на gpt-oss-120b: по умолчанию 168 токенов мыслей из 270 токенов ответа — 62% лимита впустую. С «low» осталось 12 из 118. |
| `test_reset_header_parsed` | Провайдер сообщает время сброса своим форматом, не ISO. _(×5 кейсов)_ |
| `test_router_api_dispatch_uses_api_model` | При провайдере api роутер зовёт OpenAI-клиента и подставляет модель, заданную для API. |
| `test_router_local_dispatch` | При провайдере local роутер зовёт клиента Ollama и подставляет локальное имя модели. |
| `test_save_and_load_llm_choice_api_roundtrip` | Выбор api переживает перезапуск: пишется на диск и читается обратно. |
| `test_save_llm_choice_api_requires_config` | Провайдер api без адреса и ключа сохранить нельзя. |
| `test_save_llm_choice_empty_key_keeps_existing` | Пустой ключ при повторном сохранении не затирает сохранённый (клиент присылает ключ только когда его меняют). |
| `test_save_llm_choice_local_persists` | Выбор локального провайдера сохраняется в llm.json и применяется к настройкам. |
| `test_save_llm_choice_persists_creds` | Адрес, ключ и обе модели сохраняются в llm.json — их вводят в настройках приложения, а не в .env. |
| `test_save_llm_choice_rejects_foreign_host` | Чужой провайдер не сообщает контекст — принять его значит пустить пользователя выбирать модель вслепую. |
| `test_save_llm_choice_rejects_unknown` | Неизвестное имя провайдера отвергается на уровне конфига. |
| `test_save_tpm_limits_without_saved_choice_does_not_crash` | Лимиты без сохранённого выбора записывать некуда — молча выходим. |
| `test_status_filters_unsuitable_models` | Судим по данным провайдера, а не по зашитым именам моделей. Реальный ответ Groq содержит whisper (принимает аудио), orpheus (отдаёт речь), prompt-guard (512 токенов) и allam (4096) — всё это молча сломалось бы на первой встрече, поэтому в список выбора не попадает. |
| `test_status_keeps_models_without_metadata` | Провайдер не сообщил контекст и модальности — судить не по чему, прятать нельзя: молча урезать список опаснее, чем показать лишнее. |
| `test_status_reports_context_and_sorts_by_it` | status reports context and sorts by it |
| `test_token_limit_read_from_header` | Лимит токенов в минуту берётся из заголовка ответа. В списке моделей его нет — провайдер сообщает лимит только так. Меряем при выборе модели, чтобы первая встреча шла с правильным бюджетом. |
| `test_token_limit_returns_none_when_unknown` | Проба необязательна: не вышло — работаем на запасном значении. Сеть у пользователя нестабильна (внешние API только через VPN), и упавшая проба не должна мешать сохранению настроек. _(×2 кейсов)_ |
| `test_token_limit_survives_error_response` | Заголовки лимитов приходят и с ошибкой — ответ 429 тоже годится. |
| `test_tpm_limits_persist_and_reload` | Измеренный лимит переживает перезапуск сервера — мерить каждый раз незачем. |
| `test_truncated_answer_is_an_error_not_a_silent_half` | Ответ, оборванный по пределу длины, не уходит дальше молча. На длинной встрече такой обрубок попадает в сведение фрагментов, и в протоколе не хватает куска разговора — а выглядит протокол целым. Один раз так потерялась половина заметок по третьему фрагменту, и заметили это только вручную. |
| `test_unknown_reasoning_effort_is_dropped_and_retried` | Провайдер не знает параметра — убираем и повторяем, а не падаем. Так параметр остаётся необязательным: на сервере организации с другой моделью он отвалится один раз и больше не появится. |

### `test_loadtest_metrics.py`

Путь: `server/tests/test_loadtest_metrics.py`  
Метрики нагрузочного харнесса (scripts/loadtest.py). Быстрые юнит-тесты чистых функций: ни сервера, ни моделей, ни Docker. Появились после того, как в отчёте обнаружился p95 МЕНЬШЕ медианы — наивная формула индекса на малых выборках брала минимум вместо максимума.

| Тест | Что проверяет |
|---|---|
| `test_degraded_catches_silent_transcript_loss` | Самый коварный отказ: встречи завершились штатно, но часть без транскрипта. На замерах явные ошибки плавали (18 отказов и 0 в повторе той же ступени), а недостача сегментов воспроизводилась стабильно — значит ловить надо её. |
| `test_degraded_flags_container_death` | Перезапуск контейнера и OOM считаются отказом, даже если ошибок встреч нет. |
| `test_degraded_flags_errors_and_latency` | Ошибки встреч, отсутствие распознавания и превышение p95 — тоже отказ. |
| `test_degraded_without_container_metrics` | Прогон против своего сервера: полей контейнера нет — не должно падать. |
| `test_p95_never_below_median` | Главная гарантия: 95-й перцентиль не может оказаться ниже медианы. |
| `test_p95_on_tiny_sample_takes_worst` | Регрессия: при двух замерах брался минимум, из-за чего p95 < p50. |
| `test_percentile_bounds` | Индекс не вылезает за границы списка ни при каком квантиле. |

### `test_mixer.py`

Путь: `server/tests/test_mixer.py`  
Тесты микшера каналов: выравнивание потоков, паддинг отстающего канала, RMS-доминанта для диаризации.

| Тест | Что проверяет |
|---|---|
| `test_clipping_protection` | Сумма громких каналов не выходит за пределы диапазона (нет клиппинга). |
| `test_dominance_mic_system_mixed` | Когда оба канала звучат сопоставимо громко, доминанта помечается как mixed. |
| `test_dominance_single_active_channel` | Если звучит только один канал, он и объявляется доминантой. |
| `test_dominance_spans_ignores_short_blip` | Кашель в микрофон (одно окно) посреди чужой фразы — не смена говорящего. |
| `test_dominance_spans_single_channel` | Второго канала нет — резать не по чему. |
| `test_dominance_spans_split_on_channel_flip` | Ответ без паузы: 1.2 с из звонка, потом 1.2 с в микрофон — сегмент делится на два участка ровно в точке смены канала. |
| `test_flush_drains_remainder` | Финальный flush отдаёт остаток буфера — хвост встречи не теряется. |
| `test_lagging_channel_padded_and_late_samples_dropped` | Канал отстал больше max_lag — доигрываем с тишиной, а опоздавшие семплы отбрасываем, чтобы шкала времени не разъехалась. |
| `test_single_channel_passthrough` | Системный источник выключен — после стартовой паузы (ждём второй канал не дольше max_lag) микрофон идёт как есть. |
| `test_two_channels_are_summed_aligned` | Каналы приходят разными чанками — на выходе поэлементная сумма. |

### `test_ollama_client.py`

Путь: `server/tests/test_ollama_client.py`  
Юнит-тесты клиента локальной LLM (llm/ollama_client.py) через httpx.MockTransport.

| Тест | Что проверяет |
|---|---|
| `test_generate_404_model_missing` | Отсутствующая в Ollama модель даёт понятную ошибку с подсказкой `ollama pull`. |
| `test_generate_connect_error` | Недоступный Ollama даёт понятную ошибку с адресом, а не трейсбек. |
| `test_generate_strips_think` | Блок рассуждений <think> вырезается из ответа qwen3 и не попадает пользователю. |
| `test_generate_success_builds_request` | Запрос к Ollama уходит на /api/generate с нужной моделью, системным промптом и температурой; ответ разбирается. |
| `test_status_reachable_lists_models` | Статус возвращает признак доступности и список скачанных моделей. |
| `test_status_unreachable` | Недоступный сервер даёт reachable=False и пустой список вместо исключения. |

### `test_prompts.py`

Путь: `server/tests/test_prompts.py`  
Сборка промптов: режимы встречи, право промолчать (SKIP), разбор ответа. Чистые функции без сети и без БД — самый дешёвый и самый ценный слой проверок: формулировки промпта тут единственное, что отделяет полезную подсказку от воды.

| Тест | Что проверяет |
|---|---|
| `test_allow_skip_toggles_skip_rule_everywhere` | Главная гарантия: по кнопке «Подсказать сейчас» слова SKIP быть не должно нигде — ни в system, ни в примерах, иначе модель промолчит на явный запрос. |
| `test_both_prompts_warn_about_asr_noise` | То же предупреждение и в подсказках, и в протоколе. |
| `test_detailed_prompt_carries_context_and_is_longer` | Развёрнутый вариант (для API) длиннее и включает название встречи и участников. |
| `test_every_mode_has_label_and_focus` | У каждого режима заполнены название и фокус подсказок. Разделов протокола у режимов больше нет: протокол один на все типы, а тип встречи попадает в промпт строкой в шапке. |
| `test_fragment_is_marked_as_a_fragment` | Фрагмент длинной встречи помечается, чтобы модель не искала в нём итогов. |
| `test_hint_prompt_includes_transcript_and_previous` | В промпт попадают и текущий фрагмент разговора, и ранее выданные подсказки (против повторов). |
| `test_hint_prompt_lists_all_five_categories` | В системном промпте перечислены все пять разрешённых типов подсказки. |
| `test_hint_prompt_modes_differ` | Режимы дают разные промпты; собеседование написано со стороны соискателя, переговоры — про уступки. |
| `test_normalize_mode_falls_back_to_default` | Пустой, неизвестный или отсутствующий режим (старый клиент) превращается в режим по умолчанию. |
| `test_parse_hint_keeps_real_hint_containing_skip_word` | Жадный матчинг съел бы полезную подсказку со словом skip внутри. |
| `test_parse_hint_respects_min_chars` | Слишком короткий ответ считается молчанием; порог настраивается. |
| `test_parse_hint_strips_prefix_and_restores_capital` | Служебные префиксы вроде «Подсказка:» срезаются, первая буква возвращается заглавной. |
| `test_parse_hint_treats_as_silence` | Разные формы молчания модели (SKIP, пустой ответ, «Ок.») распознаются как «подсказки нет». _(×12 кейсов)_ |
| `test_protocol_forbids_the_ways_to_lie` | Запреты против выдумок — каждый из разбора реальных протоколов. 03.08.2026: модель выдумала все шесть таймкодов, записала уже проделанное нагрузочное тестирование в планы, превратила отвергнутое предложение в решение. 09.08.2026: дважды назначила ответственным куратора, который задачу попросил, — поэтому ответственных не пишем вовсе. |
| `test_protocol_has_no_decisions_section` | Раздела решений нет намеренно: модель ставила туда описания и рекомендации через раз, а неверное «принятое решение» в протоколе хуже отсутствующего. Стоит обсудить с куратором — в исходном задании он был. |
| `test_protocol_sections_are_the_same_for_every_mode` | Разделы протокола больше не зависят от типа встречи. Своя вёрстка под каждый тип стоила 300–500 символов промпта, а при лимите 8000 токенов в минуту это столько же символов разговора, которые не влезли. Тип встречи остался строкой в шапке — модель его видит. |
| `test_protocol_warns_about_asr_noise` | Распознавание русское и коверкает английские термины: «UDP» приходит как «уд». Без предупреждения модель честно объясняет, что такое «уд». |
| `test_same_prompt_serves_the_merge` | Склейка протоколов фрагментов идёт тем же промптом: для него это просто текст, по которому надо составить протокол. |

### `test_registry.py`

Путь: `server/tests/test_registry.py`  
Юнит-тесты SpeakerRegistry — логики «кто говорит». Вместо реальных голосов используются синтетические 192-мерные векторы с точно заданной косинусной близостью: так проверяется именно решающая логика (порог, дрейф центроида, merge), а не качество ECAPA.

| Тест | Что проверяет |
|---|---|
| `test_above_threshold_matches` | Похожий выше порога голос узнаётся как тот же человек. |
| `test_below_threshold_creates_new_profile` | Голос, непохожий сильнее порога, заводит отдельный профиль, а не приписывается чужому. |
| `test_borderline_short_segment_leaves_no_print` | Пограничный коротыш («ага», узнанное со скидкой) не рождает отпечаток и не двигает существующий — источник мусорных отпечатков закрыт. |
| `test_centroid_drifts_toward_recent_voice` | Отпечаток — скользящее среднее: после серии реплик он смещается к текущему звучанию голоса (человек охрип, сменил гарнитуру). |
| `test_first_mic_voice_enrolls_self` | Первый голос, доминирующий в микрофоне, — владелец: профиль «Вы» обучается автоматически, без отдельной записи образцов. |
| `test_first_voice_creates_new_profile` | Первый услышанный голос создаёт новый профиль спикера. |
| `test_forget_removes_profile` | Удалённый профиль убирается из памяти реестра и больше не участвует в сопоставлении. |
| `test_merge_moves_prints_and_segments` | Один человек распознался как два профиля → merge переносит отпечатки и реплики, лишний профиль удаляется, данные не теряются. |
| `test_print_audio_saved_and_removed` | «Звучание» сохраняет аудио своей реплики (его можно прослушать); удаление отпечатка удаляет и файл. |
| `test_prints_capped_per_speaker` | Отпечатков на человека не больше speaker_max_prints — дальше «звучания» вливаются в ближайший отпечаток. |
| `test_recent_speaker_bonus_glues_monologue` | Монолог из звонка: близость просела чуть ниже порога, но спикер говорил только что — скидка недавности не даёт наплодить «Спикер N». |
| `test_remove_print_forgets_voice` | Удаление отпечатка: голос перестаёт узнаваться, профиль остаётся; несуществующий отпечаток — честный отказ. |
| `test_same_voice_matches_same_profile` | Повторное появление того же голоса попадает в уже существующий профиль. |
| `test_second_person_at_mic_gets_new_profile` | Два человека за одним микрофоном: непохожий голос из микрофона не приклеивается к «Вы», а получает свой профиль. |
| `test_self_bonus_keeps_owner_below_threshold` | Живой голос владельца гуляет: близость чуть ниже порога, но голос из микрофона — скидка speaker_self_bonus удерживает его в «Вы», и другое «звучание» добавляется вторым отпечатком. |
| `test_self_print_updates_only_from_mic` | Отпечаток «Вы» дообучается только голосом из микрофона: звук из звонка (даже похожий) не должен размывать профиль владельца. |
| `test_similar_voices_merge_at_low_threshold` | Обратная сторона низкого порога: голоса с близостью выше порога считаются одним человеком. При threshold=0.35 два похожих голоса (близость 0.5 — бывает у людей одного пола и тембра) склеятся. |
| `test_three_distinct_voices_stay_separate` | Три человека с непохожими голосами: реплики вперемешку, профилей — три. Случайные векторы в 192 измерениях почти ортогональны (близость ~0), у реальных непохожих голосов она 0.1–0.3 — здесь моделируется удобный случай. |

### `test_summary_format.py`

Путь: `server/tests/test_summary_format.py`  
Юнит-тесты форматирования транскрипта для резюме (llm/summary.py).

| Тест | Что проверяет |
|---|---|
| `test_build_transcript_default_limit_unchanged` | Дефолт остался прежним — поведение локального провайдера не изменилось. |
| `test_build_transcript_honours_custom_limit` | Лимит длины берётся из аргумента — так провайдеры получают разный бюджет контекста. |
| `test_build_transcript_lines_and_participants` | Каждая реплика идёт строкой «[MM:SS] Имя: текст», участники считаются по числу реплик. |
| `test_build_transcript_truncates_long` | Слишком длинный транскрипт ужимается: остаются начало и хвост, середина помечается пропуском. |
| `test_build_transcript_unknown_speaker` | Реплика без опознанного спикера подписывается «Неизвестный». |
| `test_build_transcript_without_limit_keeps_everything` | max_chars=0 — экспорт и API-провайдер получают полную расшифровку. |
| `test_mmss` | Секунды форматируются в MM:SS для меток времени в транскрипте. |

### `test_summary_generate.py`

Путь: `server/tests/test_summary_generate.py`  
Составление протокола встречи (llm/summary.generate_summary). Это ядро фичи «протокол по завершении», и до сих пор оно проверялось только косвенно — через e2e. Здесь LLM подменена фейком, поэтому тесты быстрые и проверяют именно логику: что уходит в модель, что сохраняется в БД и что происходит при ошибках.

| Тест | Что проверяет |
|---|---|
| `test_cancellation_leaves_status_to_the_replacing_task` | Отмена статус не трогает и пробрасывается дальше. Отменяют задачу только из _schedule_summary, и сразу за отменой стартует новая. Поставь мы здесь "done" — затёрли бы "summarizing" уже запущенной замены, и клиент перестал бы опрашивать посреди живой генерации. |
| `test_llm_error_saved_as_message_not_crash` | Недоступная LLM не роняет задачу: встреча закрывается с понятной ошибкой. |
| `test_long_meeting_goes_in_several_requests` | Транскрипт больше бюджета — заметки по фрагментам, потом сведение. Пауз здесь нет намеренно: ждать нужное время умеет клиент — он читает из заголовков ответа, сколько лимита осталось и когда восстановится. |
| `test_long_transcript_truncated_by_budget` | Маленький бюджет режет транскрипт, а не отправляет всё подряд. |
| `test_meeting_without_speech_is_not_sent_to_llm` | Встречу без распознанной речи в модель не отправляем — сразу ошибка. |
| `test_missing_meeting_is_ignored` | Встречу удалили, пока задача ждала очереди — тихо выходим, без исключения. |
| `test_no_tariff_limit_means_no_splitting` | У локальной модели тарифного лимита нет — деление не включается. |
| `test_short_meeting_still_goes_in_one_request` | Влезающая встреча идёт как раньше — одним запросом, без пауз. |
| `test_split_by_lines_keeps_replicas_whole` | Режем по границам реплик: разорванная пополам фраза бессмысленна. |
| `test_split_keeps_overlong_replica_in_its_own_chunk` | Реплика длиннее лимита рвать некуда — уходит своим куском целиком. |
| `test_summary_saved_with_model_name` | Успешный протокол сохраняется вместе с именем модели, встреча закрывается. |
| `test_tiny_budget_refuses_instead_of_dozens_of_requests` | Бюджет меньше промпта — честная ошибка, а не получасовое молчание. Без этой проверки транскрипт резался бы на куски по минимуму, и встреча на 40 минут превратилась бы в три десятка запросов по минуте паузы каждый — внешне неотличимо от зависания. |
| `test_transcript_and_participants_reach_the_model` | В промпт уходят реплики с таймкодами и статистика участников. |
| `test_truncated_fragment_is_retried_in_halves` | Один невезучий фрагмент не должен валить весь протокол. У рассуждающих моделей длина рассуждений гуляет, и на длинном фрагменте текста может не остаться — ответ приходит оборванным. Раньше это убивало работу минут за пять целиком; теперь фрагмент пересдаётся половинками. |
| `test_unexpected_error_also_closes_the_meeting` | Любое исключение, а не только LlmError, закрывает встречу. Раньше ловился один LlmError, поэтому таймаут по VPN или обрыв связи убивали фоновую задачу молча: встреча навсегда оставалась "summarizing", а клиент крутил спиннер, опрашивая её каждые 4 секунды. |

### `test_transcriber_junk.py`

Путь: `server/tests/test_transcriber_junk.py`  
Юнит-тесты фильтрации мусора ASR (asr/transcriber.py `_transcribe_sync`): типовые галлюцинации whisper на тишине отбрасываются. Модель не грузим — подменяем backend, load() видит его и не качает веса.

| Тест | Что проверяет |
|---|---|
| `test_junk_case_and_punctuation_dropped` | Фильтр галлюцинаций не зависит от регистра и знаков препинания. |
| `test_junk_exact_dropped` | Типичная галлюцинация ASR на тишине («Продолжение следует...») выбрасывается. |
| `test_no_parts_is_empty` | Если распознавать нечего — результат пустая строка, а не ошибка. |
| `test_parts_joined_and_empties_dropped` | Куски распознавания склеиваются через пробел, пустые пропускаются. |
| `test_real_text_kept` | Осмысленная речь фильтром не трогается. |

### `test_vad.py`

Путь: `server/tests/test_vad.py`  
Юнит-тесты буферной логики сегментатора (audio/vad.py): нарезка _cut и подрезка памяти _trim_buffer. Silero-модель не грузим — конструируем через __new__ и дёргаем чистые методы напрямую.

| Тест | Что проверяет |
|---|---|
| `test_cut_clamps_to_buffer` | Запрос за границу буфера обрезается по факту имеющихся данных, а не падает. |
| `test_cut_rejects_too_short` | Фрагмент короче порога речи не становится сегментом (щелчки и вздохи отбрасываются). |
| `test_cut_returns_segment_with_timestamps` | У вырезанного сегмента корректные метки времени, а аудио — копия, а не вид на общий буфер. |
| `test_trim_buffer_during_speech_keeps_from_start` | Во время речи буфер подрезается от начала фразы с запасом паддинга — начало реплики не теряется. |
| `test_trim_buffer_idle_keeps_recent` | В тишине буфер не растёт бесконечно: хранятся только последние секунды. |

## Компонентные (API-слой)

*REST через FastAPI TestClient в одном процессе*

### `test_api.py`

Путь: `server/tests/test_api.py`  
Компонентные тесты REST-API: эндпоинты через FastAPI TestClient. Это НЕ функциональные тесты «чёрным ящиком», и важно не выдавать их за таковые: TestClient вызывает приложение внутри процесса (без сети и без живого сервера), а данные для read-эндпоинтов засеваются прямо в БД через crud — то есть в обход публичного интерфейса. Проверяются коды ответов и форма данных. Функциональную проверку — систему целиком глазами пользователя — даёт tests/test_e2e_live.py: настоящий uvicorn, WebSocket, встреча от аудио до REST.

| Тест | Что проверяет |
|---|---|
| `test_asr_state` | /api/asr сообщает текущий движок, модель и список доступных моделей по движкам. |
| `test_delete_meeting` | Встреча удаляется, повторное удаление даёт 404. |
| `test_delete_missing_speaker_404` | Удаление несуществующего спикера даёт 404. |
| `test_delete_missing_voiceprint_404` | Удаление несуществующего отпечатка даёт 404. |
| `test_delete_self_speaker_forbidden` | Профиль владельца «Вы» удалить нельзя — 400. |
| `test_export_md_and_txt` | Выгрузка встречи работает в обоих форматах с правильным Content-Type и содержит транскрипт. |
| `test_export_unknown_meeting_404` | Выгрузка несуществующей встречи даёт 404. |
| `test_health` | /api/health отдаёт статус сервера, состояние ASR, диаризации и выбранного провайдера LLM. |
| `test_llm_get` | /api/llm отдаёт состояние провайдера LLM — и намеренно не содержит API-ключ. |
| `test_llm_probe_filters_models` | В списке выбора остаются только пригодные модели, негодные посчитаны. |
| `test_llm_probe_models` | /api/llm/models запрашивает список моделей по введённым (ещё не сохранённым) кредам — для выпадающего списка в настройках. |
| `test_llm_probe_rejects_unsupported_host` | Чужой провайдер не сообщает размер контекста — принять его значит пустить пользователя выбирать модель вслепую. |
| `test_llm_set_api_unconfigured_400` | Включить внешний API без адреса и ключа нельзя — 400 с объяснением. |
| `test_llm_set_api_with_creds` | Адрес, ключ и модели принимаются из настроек приложения; ключ в ответе не возвращается. |
| `test_llm_set_local_ok` | Переключение на локальную модель принимается всегда: ей не нужны ни адрес, ни ключ. |
| `test_llm_set_unknown_provider_400` | Неизвестный провайдер отклоняется с кодом 400, а не сохраняется молча. |
| `test_llm_state_offers_default_base_url` | Клиент подставляет адрес в поле из ответа сервера, а не хранит свою копию — иначе она однажды разъедется со списком разрешённых хостов. |
| `test_meeting_detail_reports_mode` | Карточка встречи отдаёт тип встречи — клиент показывает его чипом. |
| `test_meetings_list_and_get` | Список и карточка встречи отдают сохранённые данные; несуществующий id даёт 404. |
| `test_merge_keeps_richer_profile_and_moves_segments` | Целевым становится профиль с бо́льшим числом реплик (см. registry.merge), реплики второго переезжают к нему, сам он исчезает из списка. |
| `test_merge_needs_two_speakers` | Слияние профилей требует ровно двух id — иначе 400. |
| `test_merge_same_speaker_rejected` | Слить профиль сам с собой нельзя — 400. |
| `test_speaker_rename` | Спикер переименовывается; для несуществующего id — 404. |
| `test_startup_closes_meetings_stuck_in_summarizing` | Сервер убили посреди резюме — при следующем старте встреча не висит. Довести её до конца больше некому: фоновая задача умерла вместе с процессом, а клиент опрашивал бы статус «summarizing» до бесконечности. |
| `test_summarize_live_conflict` | Составить протокол по ещё идущей встрече нельзя — 409. |
| `test_summarize_sets_status` | Запрос протокола переводит встречу в статус summarizing; для несуществующей встречи — 404. |
| `test_voiceprint_audio_404_when_absent` | Аудио несуществующего отпечатка даёт 404, а не падение с трейсбеком. |

## Интеграционные (БД)

*миграции схемы на реальном файле SQLite*

### `test_migrations.py`

Путь: `server/tests/test_migrations.py`  
Интеграционные тесты миграций схемы БД (db/database.py) на реальном файле SQLite: старая схема → миграция → апгрейд без потери данных. Быстрые (без моделей), поэтому идут в дефолтном прогоне.

| Тест | Что проверяет |
|---|---|
| `test_meeting_mode_migration_runs_before_autoincrement` | Регрессия: _migrate_autoincrement строит SELECT по колонкам актуальной модели, поэтому на старой базе без meeting_mode он падает с «no such column» — прямо при старте сервера. Колонку обязана добавить предыдущая миграция. |
| `test_migrate_autoincrement_rebuilds_and_preserves` | Старая таблица без AUTOINCREMENT перестраивается без потери данных (иначе id удалённой встречи мог достаться новой). |
| `test_migrate_meeting_mode_adds_column_and_backfills` | Колонка типа встречи добавляется к старой базе, существующие встречи помечаются как планёрки; повторный запуск безопасен. |
| `test_migrate_samples_adds_columns_and_merges` | Аудио-образцы сливаются с отпечатками в одно «звучание», лишние файлы удаляются. |
| `test_migrate_voiceprints_moves_centroid` | Центроид голоса переезжает из строки спикера в отдельную таблицу отпечатков. |

## Интеграционные (модели)

*реальная ECAPA на синтезированных голосах*

### `test_diarization_integration.py`

Путь: `server/tests/test_diarization_integration.py`  
Интеграционный тест: реальная ECAPA + три синтезированных голоса macOS. Запуск (небыстрый, грузит модель): .venv/bin/python -m pytest -m integration Проверяет всю цепочку «звук → эмбеддинг → профиль» на трёх разных голосах. Синтетические голоса стабильнее человеческих, поэтому это дымовой тест пайплайна; для оценки живых голосов есть scripts/eval_voices.py.

| Тест | Что проверяет |
|---|---|
| `test_current_threshold_separates_three_voices` | С порогом из конфига три голоса дают ровно три профиля, без раздвоений и склеек. |
| `test_own_phrases_closer_than_foreign` | Худшая близость «своих» фраз должна быть выше лучшей «чужой» — иначе никакой порог не разделит эти голоса. |
| `test_speaker_echo_yields_single_segment` | Эхо из колонок: голос из звонка играет в комнате и попадает в микрофон с задержкой. В смешанном потоке это ОДИН сегмент (копия совпадает по времени с оригиналом), и доминанта — системный канал. |

## E2E (системные)

*живой uvicorn + WebSocket: встреча целиком, как у пользователя*

### `test_e2e_live.py`

Путь: `server/tests/test_e2e_live.py`  
E2E: одноразовый живой сервер + WebSocket-клиент, как настоящее приложение. Поднимает uvicorn на :8766 с временной папкой данных (модели — симлинком из server/data/models, чтобы не перекачивать веса; БД и голоса одноразовые — живая база не затрагивается). Прогоняет встречи целиком: синтезированный звук → микшер → VAD → ASR → диаризация → события → REST. Ollama намеренно недоступна — проверяется честная ошибка резюме (живое резюме — в ручном чек-листе TESTING.md). Запуск (медленный, несколько минут): .venv/bin/python -m pytest -m e2e Тесты зависят друг от друга по данным и выполняются по порядку — база накапливает спикеров и встречи, как при реальном использовании.

| Тест | Что проверяет |
|---|---|
| `test_01_speaker_echo_no_duplicates` | Звонок на колонках: голос собеседника и его эхо в микрофоне → один сегмент, приписан НЕ «Вы», канал — system. |
| `test_02_self_enrolls_and_is_remembered` | Голос из микрофона становится «Вы»; на следующей встрече тот же голос узнаётся даже из системного канала — база голосов живёт между встречами. |
| `test_03_two_voices_and_short_reply` | Два голоса в одной встрече → два разных профиля (Милена — та же, что во встрече №1, владелец узнан по голосу); короткая реплика (<0.4 с) приклеивается к последнему говорившему. |
| `test_04_guest_gets_new_profile` | Незнакомый голос получает новый профиль, не примазываясь ни к «Вы», ни к Милене. |
| `test_05_silent_meeting` | Тишина всю встречу: ноль сегментов, встреча завершается, резюме честно сообщает, что распознавать нечего. |
| `test_06_long_monologue_is_split` | Монолог длиннее лимита сегмента (15 с) режется на части, стоп дожимает хвост, встреча завершается. |
| `test_07_ws_abort_finalizes_meeting` | Клиент оборвался без stop (закрыли крышку ноутбука) — встреча всё равно завершается на сервере и не остаётся вечно live. |
| `test_08_summary_error_is_reported` | Ollama недоступна → у встречи с речью появляется понятная ошибка резюме (полный цикл с живой Ollama — в ручном чек-листе TESTING.md). |
| `test_09_rest_operations` | Переименование, merge, экспорт, удаление — поверх накопленных данных. |
| `test_10_asr_switch_rules` | Переключение модели: 409 во время live-встречи, 400 на невалидную пару, выбор персистится в asr.json. |

## Нагрузочные

*параллельные встречи, задержка и память*

### `test_load_smoke.py`

Путь: `server/tests/test_load_smoke.py`  
Дымовой нагрузочный тест (-m load, исключён из дефолтного прогона): две параллельные встречи проходят целиком без ошибок. Полноценный прогон — скриптом scripts/loadtest.py. Требует кэш моделей server/data/models.

| Тест | Что проверяет |
|---|---|
| `test_two_concurrent_meetings_survive` | Две одновременные встречи проходят целиком без ошибок, память измерима — дымовая проверка перед полноценной нагрузкой. |

## Клиент (Vitest)

*чистая логика фронтенда в jsdom, без браузера*

### `live.test.ts`

Путь: `client/src/api/live.test.ts`  
LiveClient.sendAudio; LiveClient.start; LiveClient.ask

| Тест | Что проверяет |
|---|---|
| `ставит байт канала первым и копирует PCM следом` | ставит байт канала первым и копирует PCM следом |
| `молчит, если сокет не открыт` | молчит, если сокет не открыт |
| `передаёт тип встречи серверу (от него зависят промпты)` | передаёт тип встречи серверу (от него зависят промпты) |
| `шлёт вопрос вместе с выделенными репликами` | шлёт вопрос вместе с выделенными репликами |
| `выделение необязательно — вопрос может быть общим` | выделение необязательно — вопрос может быть общим |

### `rest.test.ts`

Путь: `client/src/api/rest.test.ts`  
rest.request

| Тест | Что проверяет |
|---|---|
| `возвращает разобранный JSON при успехе` | возвращает разобранный JSON при успехе |
| `бросает body.detail при ошибке` | бросает body.detail при ошибке |
| `падает на` | падает на |

### `capture.test.ts`

Путь: `client/src/audio/capture.test.ts`  
looksLikeLoopback

| Тест | Что проверяет |
|---|---|
| `узнаёт виртуальные loopback-драйверы` | узнаёт виртуальные loopback-драйверы |
| `не путает обычные устройства с loopback` | не путает обычные устройства с loopback |

### `Transcript.test.ts`

Путь: `client/src/components/Transcript.test.ts`  
formatTime

| Тест | Что проверяет |
|---|---|
| `форматирует секунды меньше минуты` | форматирует секунды меньше минуты |
| `форматирует минуты:секунды` | форматирует минуты:секунды |
| `добавляет часы начиная с 3600 с` | добавляет часы начиная с 3600 с |
| `зажимает отрицательные к нулю и отбрасывает дробь` | зажимает отрицательные к нулю и отбрасывает дробь |

### `store.test.ts`

Путь: `client/src/store.test.ts`  
store

| Тест | Что проверяет |
|---|---|
| `getServerUrl падает на дефолт без сохранённого адреса` | getServerUrl падает на дефолт без сохранённого адреса |
| `getServerUrl срезает завершающие слеши` | getServerUrl срезает завершающие слеши |
| `getSetting/setSetting работают через префикс stenograf.` | getSetting/setSetting работают через префикс stenograf. |
| `isDebugMode отражает флаг` | isDebugMode отражает флаг |

## Чего здесь нет

UI-страницы (`LivePage`, `SettingsPage`, `SpeakersPage`) юнит-тестами не покрыты — они проверяются `tsc --noEmit`, сборкой `vite build` и ручным чек-листом [TESTING.md](TESTING.md). Там же то, что автоматика проверить не может в принципе: реальный микрофон, эхо колонок, упакованное приложение.

