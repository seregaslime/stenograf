# Отчёт о нагрузочном тестировании — Стенограф

Сводка всех нагрузочных прогонов: что мерили, чем, что получилось и какие
выводы для эксплуатации. Каждый раздел ссылается на скрипт, которым результат
воспроизводится.

**Условия замеров:** Apple M3, Docker Desktop, контейнер `linux/arm64` с лимитом
памяти 3 ГБ, движок ASR GigaAM `v3_e2e_rnnt` на CPU, синтезированная непрерывная
русская речь (macOS `say`). Нагрузка подаётся **на контейнер**, чтобы рабочая
машина оставалась свободной.

---

## Оглавление

1. [Живые встречи — сколько человек тянет](#1-живые-встречи)
2. [Пачка встреч — предел до отказа](#2-пачка-встреч)
3. [Обстрел REST-API](#3-обстрел-rest-api)
4. [Библиотека голосов — рост O(N)](#4-библиотека-голосов)
5. [Долгая встреча — утечки](#5-долгая-встреча)
6. [Найденные дефекты](#6-найденные-дефекты)
7. [Выводы для эксплуатации](#7-выводы-для-эксплуатации)
8. [Что не измерено](#8-что-не-измерено)

---

## 1. Живые встречи

**Скрипт:** `scripts/loadtest.py --realtime`
**Что меряем:** аудио идёт в реальном времени, как от настоящего клиента.
«Отставание» — через сколько секунд после конца речи реплика появляется в
транскрипте.

| Встреч | Отставание p50 | Отставание p95 | Память | Пригодность |
|---:|---:|---:|---:|---|
| 2 | 1.4 c | **1.7 c** | 2229 МБ | ✅ ощущается живым |
| 4 | 3.8 c | **4.7 c** | 2262 МБ | ✅ рабочий предел |
| 6 | 10.0 c | 11.7 c | 2378 МБ | ⚠️ заметно отстаёт |
| 8 | 14.3 c | 16.7 c | 2587 МБ | ⚠️ уже не «реальное время» |
| 10 | 18.8 c | 22.1 c | 2633 МБ | ❌ |
| 14 | 28.9 c | 33.2 c | 2743 МБ | ❌ |

Ошибок не было ни на одной ступени: сервер не падает, а **плавно отстаёт**.

**Ёмкость одного процесса — 3–4 одновременные встречи.**

> Мерилось на непрерывной речи — худший случай. В реальной встрече есть паузы,
> VAD тишину отбрасывает, до ASR доходит 50–70% времени. Живых встреч машина
> потянет примерно в полтора раза больше.

## 2. Пачка встреч

**Скрипт:** `scripts/loadtest.py --ramp`
**Что меряем:** аудио летит в 20 раз быстрее живого клиента — это глубина
очереди и пропускная способность, а НЕ число живых встреч.

| Встреч | p50 | p95 | Сегментов | Память | Ошибок |
|---:|---:|---:|---:|---:|---:|
| 60 | 40.6 c | 55.9 c | 60 ✅ | 2819 МБ | 0 |
| 80 | 52.0 c | 71.6 c | 80 ✅ | 3057 МБ | 0 |
| 100 | 61.0 c | 89.1 c | 100 ✅ | 2616 МБ | 0 |
| **120** | 78.1 c | 130.9 c | **120 ✅** | 2678 МБ | 0 |
| 140 | 89.9 c | 142.4 c | **123 ⚠️** | 2838 МБ | **0** |

**Предел — 120 встреч в пачке.** На 140 отказ проявляется не ошибками, а
**потерей транскриптов**: 17 встреч завершились штатно, но без единого сегмента.

⚠️ Числа в разделах 1 и 2 различаются в 30 раз и **описывают разное**. Для
сайзинга годится только раздел 1.

Пропускная способность стабильна: **5–7× реального времени** независимо от числа
встреч — упирается лок ASR.

## 3. Обстрел REST-API

**Скрипт:** `scripts/spamtest.py --ramp`
**Что меряем:** параллельные HTTP-запросы к читающим эндпоинтам.

| Одновременно | Запросов | RPS | p50 | p95 | Отказов | 429 | 5xx |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 10 | 500 | 67 | 25.7 мс | 336 мс | 0 | 0 | 0 |
| **50** | 500 | **210** | 129.5 мс | 471 мс | 0 | 0 | 0 |
| 90 | 500 | 144 | 536.9 мс | 1.0 c | 0 | 0 | 0 |
| 130 | 650 | 70 | 905.2 мс | 5.2 c | 0 | 0 | 0 |

**Ни одного отказа ни на одной ступени** — все ответы 200.

Пик пропускной способности — **210 rps при 50 одновременных**. Дальше RPS
падает при растущей задержке: классическое насыщение, очередь растёт, полезной
работы делается меньше. Для сотни пользователей, листающих историю встреч, запас
избыточный.

Эндпоинты, ходящие во внешний LLM-API, из обстрела исключены: это платные
запросы к провайдеру.

## 4. Библиотека голосов

**Скрипт:** `scripts/bench_registry.py`
**Что меряем:** `match_all` перебирает всех спикеров и все их отпечатки — единственное
место в конвейере с алгоритмической сложностью O(спикеры × отпечатки).

| Спикеров | Отпечатков | Опознание p50 | мкс/отпечаток | Память библиотеки |
|---:|---:|---:|---:|---:|
| 10 | 50 | 0.022 мс | 0.44 | 37 КБ |
| 100 | 500 | 0.185 мс | 0.37 | 375 КБ |
| 500 | 2500 | 0.932 мс | 0.37 | 1.8 МБ |
| 1000 | 5000 | **1.846 мс** | 0.37 | 3.7 МБ |

Рост **строго линейный** — гипотеза O(N) подтвердилась. Но **узким местом это не
является**: даже при 1000 сотрудников опознание стоит 1.8 мс против ~1400 мс у
распознавания, то есть **0.1% работы**. Чтобы диаризация стала заметной статьёй
расхода, нужно порядка **75 000 спикеров**.

Ценность замера в том, что теперь это проверяемое утверждение, а не догадка.

## 5. Долгая встреча

**Скрипт:** `scripts/soaktest.py --minutes 60`
**Что меряем:** одна встреча в реальном времени на протяжении часа; память
контейнера во времени, наклон прямой (МБ/час), стабильность скорости обработки.

Зачем отдельно: остальные прогоны длятся десятки секунд, за такое время утечка
не видна. Утечка — тот класс проблем, который **не лечится докупкой памяти**:
больший объём лишь отодвигает падение.

| Показатель | Значение |
|---|---:|
| Длительность | 60 мин |
| Распознано реплик | 234 |
| Память в начале → в конце | 2776 → 2817 МБ |
| Пик памяти | 2848 МБ |
| **Рост памяти** | **+46.1 МБ/час** |
| Реплик/мин в начале | 4.1 |
| Реплик/мин в конце | **4.1** |
| Перезапусков / OOM | 0 / нет |

**Вердикт: ⚠️ требует внимания, но не приговор.**

Хорошее: **скорость обработки не просела** — 4.1 реплики в минуту и в начале, и
в конце часа. Сервер не накапливает отставание.

Настораживающее: **+46 МБ/час**. За сутки непрерывной работы это ~1.1 ГБ — при
лимите 3 ГБ и базовых 2.8 ГБ контейнер упрётся в потолок примерно через сутки.

Почему это ещё не доказанная утечка: час — короткий срок, и часть роста может
быть выходом аллокатора Python/PyTorch на плато. Наклон считается по второй
половине прогона (первые минуты отбрасываются как прогрев), но и этого может не
хватать. Дека `_recent` при 234 репликах даже не заполнилась (потолок 400), так
что она не при чём — подозрение на аллокатор или сессию SQLAlchemy.

**Что делать:** прогнать 4–6 часов и посмотреть, выйдет ли кривая на плато.
Скрипт для этого готов, менять ничего не надо:

```bash
.venv/bin/python scripts/soaktest.py --minutes 300 --report reports/soaktest.md
```

Сырые данные с точками замера — в `server/reports/soaktest.md`.

## 6. Найденные дефекты

Нагрузочное тестирование нашло три дефекта, все исправлены.

### 6.1. p95 меньше медианы

В отчётах встречалось `p95 = 3.7 c` при `p50 = 4.5 c` — 95-й перцентиль не может
быть ниже медианы. Причина: индекс считался как `int(n × 0.95) − 1`, что на малых
выборках даёт **минимум вместо максимума** (при n=2 это индекс 0).

Исправлено на «ближайший ранг» `ceil(q × n) − 1`; закрыто параметризованным
тестом на выборках от 1 до 39 элементов.

### 6.2. Тихая потеря транскриптов

На 130–140 встречах явные ошибки оказались **нестабильными**: 18 отказов в одном
прогоне и **0 в повторе той же ступени**. А недостача сегментов воспроизводилась
всегда.

Это худший вид отказа: встреча завершается штатно, клиент сбоя не видит, а
расшифровки нет. Детектор деградации переписан — теперь ловит `сегментов <
встреч`, а не только исключения.

### 6.3. Нагрузка мерила не то, что заявляла

Кадр в 100 мс отправлялся каждые 5 мс — **в 20 раз быстрее живого клиента**.
Полученные «120 одновременных встреч» описывали глубину очереди, а не ёмкость.
Добавлен режим `--realtime`; разница в выводах — 30-кратная.

## 7. Требования к железу

### Главный принцип: считать ядрами, а не гигабайтами

При росте с 2 до 14 встреч память выросла с 2.2 до 2.7 ГБ, а задержка — **в 20
раз**. Модель в памяти одна на всех, на встречу приходятся только буферы.
Транскрайбер один на процесс и защищён локом (`Transcriber._infer_lock`), поэтому
добавить контейнеру памяти — очередь не уменьшится ни на секунду.

```
процессов = (ядра − 1) / 3          # одно ядро оставляем системе
ёмкость   = процессов × 3.5 встречи
```

### Опорные цифры

| Что | Значение |
|---|---|
| Память под модели (простой) | **1.9 ГБ** |
| Память на каждую встречу сверх этого | ~45 МБ |
| Процессор при полной загрузке | **2.5–2.7 ядра** на процесс |
| Пропускная способность ASR | 5–7× реального времени |
| Комфортная ёмкость | **3–4 встречи** на процесс |

### Конфигурации

| | Минимум | Рекомендуемая | Организация 100+ |
|---|---|---|---|
| **Кому** | демо, отдел до 4 чел. | отдел 20–30 чел. | 100+ сотрудников |
| **CPU** | 4 ядра | **8 ядер** | 16 ядер |
| **RAM** | **4 ГБ** | 8 ГБ | 16 ГБ |
| **Диск** | 10 ГБ SSD | 50 ГБ SSD | 200 ГБ SSD |
| **Процессов** | 1 | 2 | 4 |
| **Одновременных встреч** | 3–4 | 6–8 | 12–16 |

ОС во всех случаях — Linux с Docker. Минимальная конфигурация — это ровно то, на
чём сделаны замеры выше (контейнер с `mem_limit: 3g`).

Расчёт «человек на конфигурацию» исходит из того, что одновременно совещается
20–30% сотрудников. Если у вас культура сплошных созвонов — считайте по числу
одновременных встреч, а не по штату.

### Диск

| Что | Сколько |
|---|---|
| Модели (GigaAM + ECAPA) | ~2 ГБ, один раз |
| Образ сервера | 2.1 ГБ |
| Транскрипт встречи | ~50 КБ на час |
| **Запись аудио** (если включена) | **230 МБ на час встречи** |

Запись аудио — главный потребитель диска: два канала PCM 16 кГц. При 20 встречах
в день по часу это ~4.6 ГБ в сутки. Если запись не нужна, лучше выключить.

С учётом замера из раздела 5 (+46 МБ/час) для круглосуточной работы стоит либо
перезапускать сервер раз в сутки, либо заложить запас памяти сверх таблицы.

### Сервер деградирует плавно

За все прогоны контейнер **ни разу не упал**: статус `running`, 0 перезапусков,
`OOMKilled: false`. Под перегрузкой он отбраковывает часть работы и продолжает
обслуживать остальных — падения и потери данных не происходит.

## 8. Что не измерено

Честный список, чтобы числами не пользовались шире, чем они верны:

- **Подсказки и протокол под нагрузкой.** Все прогоны с выключенными `hints` и
  `summarize` — LLM не участвовал. При включённых подсказках добавляется сетевая
  задержка и общий на всех rate-limit: ключ хранится на сервере, значит у всех
  сотрудников организации он один.
- **Запись аудио под нагрузкой.** Все прогоны с `record_audio: false`, диск не
  нагружался. Запись стоит ~230 МБ на час встречи.
- **Архитектура x86.** Мерилось на ARM64 (Apple M3). На серверных x86 числа
  будут другими — прогон надо повторить на целевом железе.
- **Точность предела.** Рампа шла шагом 20, поэтому «120» означает «между 120 и
  140». Для точного числа есть режим `--refine` (бинарный поиск) и `--repeats`
  (повтор ступеней), но длинный прогон с ними ещё не делался.

---

## Как воспроизвести всё

```bash
docker compose up -d --no-deps server
cd server

.venv/bin/python scripts/loadtest.py --url http://127.0.0.1:8765 \
    --container practika-server-1 --meetings 4 --seconds 20 --realtime --json
.venv/bin/python scripts/loadtest.py --url http://127.0.0.1:8765 \
    --container practika-server-1 --ramp --report reports/loadtest.md
.venv/bin/python scripts/spamtest.py --url http://127.0.0.1:8765 \
    --ramp --report reports/spamtest.md
.venv/bin/python scripts/bench_registry.py
.venv/bin/python scripts/soaktest.py --minutes 60 --report reports/soaktest.md
```

Метрики самих скриптов покрыты тестами: `tests/test_loadtest_metrics.py`,
`tests/test_bench_registry.py`.
