# Python
server/.venv/
__pycache__/
*.pyc
server/data/
server/.env

# Тесты / покрытие (генерируется прогонами)
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
client/coverage/
# Артефакты прогона (JUnit XML, базы покрытия, HTML-отчёт).
# Сами TEST_REPORT.md и TESTS.md коммитятся — они генерируются из этих артефактов.
server/reports/

# Node / Electron
client/node_modules/
client/dist/
client/dist-electron/
client/release/

# OS
.DS_Store
.claude/
server.log
