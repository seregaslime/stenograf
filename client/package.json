{
  "name": "stenograf-client",
  "productName": "Стенограф",
  "private": true,
  "version": "0.2.0",
  "description": "Транскрипция встреч в реальном времени с локальной LLM",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "dev:app": "concurrently -k \"npm:dev\" \"npm:electron-dev\"",
    "electron-dev": "wait-on tcp:5173 && cross-env ELECTRON_START_URL=http://localhost:5173 electron .",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "dist:mac": "npm run build && electron-builder --mac dmg zip",
    "dist:win": "npm run build && electron-builder --win nsis portable --x64",
    "lint": "eslint src"
  },
  "build": {
    "appId": "ru.seregaslime.stenograf",
    "productName": "Стенограф",
    "artifactName": "Stenograf-${version}-${os}-${arch}.${ext}",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**",
      "electron/**",
      "package.json"
    ],
    "asar": false,
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "identity": null,
      "category": "public.app-category.productivity",
      "extendInfo": {
        "NSMicrophoneUsageDescription": "Стенограф превращает вашу речь на встрече в текст.",
        "NSAudioCaptureUsageDescription": "Стенограф записывает системный звук, чтобы транскрибировать голоса собеседников в звонке."
      }
    },
    "win": {
      "target": [
        "nsis",
        "portable"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "portable": {
      "artifactName": "Stenograf-${version}-portable-${arch}.${ext}"
    }
  },
  "dependencies": {
    "electron-audio-loopback": "^1.0.6",
    "marked": "^14.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "@vitest/coverage-v8": "^2.1.8",
    "concurrently": "^9.0.0",
    "cross-env": "^7.0.3",
    "electron": "^35.0.0",
    "electron-builder": "^26.15.3",
    "eslint": "^10.8.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.67.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.8",
    "wait-on": "^8.0.0"
  }
}
